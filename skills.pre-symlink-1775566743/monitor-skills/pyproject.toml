[project]
name = "pi-skill-monitor-skills"
version = "0.1.0"
description = "Runtime package for the monitor-skills skill"
requires-python = ">=3.11,<3.13"
dependencies = [
    "httpx>=0.27",    "loguru>=0.7.0,<0.7.3",
    "typer>=0.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
