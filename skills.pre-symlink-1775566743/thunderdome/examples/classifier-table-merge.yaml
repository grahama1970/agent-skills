name: table-merge-classifier
description: "Classify whether adjacent PDF tables should merge — paired images"
data_dir: /home/graham/workspace/experiments/pi-mono/.pi/skills/create-table-classifier/data/merge_images/split
skill: classifier-lab

scoring:
  metric_path: "$.selected_metrics.macro_f1"
  metric_regex: "macro_f1[:\\s=]+([0-9.]+)"
  gate_threshold: 0.90
  direction: higher_better

convergence:
  max_rounds: 5
  n_strategies: 3
  plateau_window: 3
  plateau_epsilon: 0.02

# No strategies section — generated from /analytics + /dogpile research

reviewers:
  - name: brandon-bailey
  - name: tim-blazytko

dogpile_on_failure: true
memory_scope: classifier-lab
