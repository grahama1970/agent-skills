[project]
name = "cleanup"
version = "0.2.0"
description = "Deep codebase assessment and technical debt cleanup with 12TB archive support"
requires-python = ">=3.11,<3.13"

dependencies = ["typer>=0.9.0"]
