You are analyzing failed skill compositions in Embry-OS.

Given these execution traces where skill "{skill_a}" was chained with skill "{skill_b}":
- Total executions: {total}
- Success rate: {success_rate:.0%}
- Errors encountered: {errors}
- Example chains: {chains}

Distill this into a COUNTERFACTUAL LESSON — what went wrong and what should be done instead.

Respond with JSON only:
{{
  "lesson": "1-2 sentence failure explanation",
  "root_cause": "why this composition fails",
  "alternative": "what should be used instead (or 'none' if the pair is just incompatible)",
  "affinity_weight": 0.0-1.0
}}