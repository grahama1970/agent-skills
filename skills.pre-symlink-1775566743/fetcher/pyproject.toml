[project]
name = "pi-skill-fetcher"
version = "0.1.0"
description = "Runtime package for the fetcher skill"
requires-python = ">=3.11,<3.13"
dependencies = [    "duckdb",
    "typer>=0.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
