# prompt-health-auditor

Use `persona.yaml` as the authoritative Petey subagent contract.

Petey owns `prompt_health` queue issues and must run before Qbert/QRA lanes.
Petey must not run QRA generation or database mutation.

