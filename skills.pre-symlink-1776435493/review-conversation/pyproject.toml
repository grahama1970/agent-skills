[project]
name = "review-conversation"
version = "1.0.0"
description = "Review SPARTA conversation transcripts and grading with full transparency"
requires-python = ">=3.11,<3.13"
dependencies = [
    "typer>=0.9.0",
    "rich>=13.0.0",
    "loguru>=0.7.0,<0.7.3",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
test = ["pytest"]
