[project]
name = "pi-skill-learn-voice"
version = "0.1.0"
description = "Runtime package for the learn-voice skill"
requires-python = ">=3.11,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
