[project]
name = "monitor-pi"
version = "1.0.0"
description = "Pi agent infrastructure health watchdog"
requires-python = ">=3.11,<3.13"
dependencies = [
    "loguru>=0.7.0,<0.7.3",
    "typer",
    "httpx",
]

[dependency-groups]
dev = []
