[project]
name = "treesitter-skill"
version = "1.0.0"
description = "Parse and extract code symbols using tree-sitter"
requires-python = ">=3.10,<3.13"

dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
