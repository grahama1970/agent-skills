[project]
name = "corpus-report"
version = "0.1.0"
description = "Report on the continuous PDF extraction learning corpus"
requires-python = ">=3.11,<3.13"
dependencies = [
    "typer>=0.9.0",
    "loguru>=0.7.0,<0.7.3",
    "rich>=13.0.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=7.4.0",
]
