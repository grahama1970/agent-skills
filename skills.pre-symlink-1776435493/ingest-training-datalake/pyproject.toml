[project]
name = "pi-skill-ingest-training-datalake"
version = "0.1.0"
description = "Runtime package for the ingest-training-datalake skill"
requires-python = ">=3.11,<3.13"
dependencies = [    "loguru>=0.7.0,<0.7.3",
    "typer>=0.9.0",
]
