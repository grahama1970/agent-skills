[project]
name = "pi-skill-create-annotated-pdf"
version = "0.1.0"
description = "Runtime package for the create-annotated-pdf skill"
requires-python = ">=3.11,<3.13"
dependencies = [
    "loguru>=0.7.0,<0.7.3",
    "pymupdf",
    "typer>=0.9.0",
]
