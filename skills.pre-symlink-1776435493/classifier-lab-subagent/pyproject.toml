[project]
name = "classifier-lab-subagent"
version = "1.0.0"
description = "3-concurrent subagent classifier training experiment"
requires-python = ">=3.11,<3.13"
dependencies = [
    "httpx>=0.27",
    "typer>=0.12",
    "loguru>=0.7,<0.7.3",
    "datasets>=4.0",
]

