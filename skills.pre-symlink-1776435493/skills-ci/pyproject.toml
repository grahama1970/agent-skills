[project]
name = "pi-skill-skills-ci"
version = "0.1.0"
description = "Runtime package for the skills-ci skill"
requires-python = ">=3.11,<3.13"
dependencies = [
    "httpx>=0.27","loguru>=0.7.0,<0.7.3", "pyyaml>=6.0", "requests", "typer>=0.9", "python-dotenv>=1.0"]

[tool.hatch.build.targets.wheel]
only-include = ["skills_ci.py"]
