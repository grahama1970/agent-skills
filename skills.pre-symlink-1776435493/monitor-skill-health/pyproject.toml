[project]
name = "monitor-skill-health"
version = "0.1.0"
description = "Nightly skill-quality monitor with aggregate summary reporting"
requires-python = ">=3.11,<3.13"
dependencies = [
    "typer>=0.9.0",
    "rich>=13.0.0",
    "loguru>=0.7.0,<0.7.3",
    "python-dotenv>=1.0.0",
]

[tool.uv]
dev-dependencies = []
