[project]
name = "ingest-website"
version = "0.1.0"
description = "Ingest an entire website into /memory as a RAG resource"
requires-python = ">=3.11,<3.13"
dependencies = [
    "httpx>=0.27",
    "beautifulsoup4>=4.12",
    "loguru>=0.7,<0.7.3",
    "rich>=13",
    "typer>=0.12",
]
