[project]
name = "recommend-skill-chain"
version = "0.1.0"
description = "Shadow-LEGO cascade recommender for skill chain composition"
requires-python = ">=3.11,<3.13"
dependencies = [
    "httpx>=0.27","joblib", "loguru>=0.7.0,<0.7.3", "rich", "scikit-learn", "typer"]

[project.scripts]
recommend-skill-chain = "src.cli:app"
