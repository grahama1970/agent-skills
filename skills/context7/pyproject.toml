[project]
name = "context7"
version = "0.1.0"
description = "Fetch up-to-date library documentation from Context7 API"
requires-python = ">=3.11"

dependencies = [
    "typer>=0.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
