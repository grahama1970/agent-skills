[project]
name = "webgpt"
version = "0.1.0"
description = "Emit deterministic surf CLI commands for WebGPT interactions"
requires-python = ">=3.12"
dependencies = ["python-dotenv>=1.0", "typer>=0.9.0"]

[project.scripts]
webgpt = "scripts.webgpt_cli:app"
