[project]
name = "agentic-evals"
version = "0.1.0"
description = "Deterministic multi-trial agentic evaluation runner for skill fixtures."
requires-python = ">=3.11"
dependencies = [
  "typer>=0.12.0",
]

[tool.uv]
package = false
