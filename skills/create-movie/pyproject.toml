[project]
name = "create-movie"
version = "0.1.0"
description = "Orchestrated movie creation for Horus persona"
requires-python = ">=3.11"
dependencies = [
    "click>=8.1.0",
    "rich>=13.0.0",
    "pydantic>=2.0.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
]
