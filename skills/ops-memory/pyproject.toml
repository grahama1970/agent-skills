[project]
name = "ops-memory"
version = "0.1.0"
description = "Natural-language front door + health/metrics for the /memory stack (composes ops-arango, ops-qdrant, memory, phart-dag-chart)."
requires-python = ">=3.11"
dependencies = [
    "typer>=0.12.0",
    "loguru>=0.7.0",
]

[tool.uv]
package = false
