[project]
name = "monitor-projects"
version = "0.1.0"
description = "Nightly roundtable review of skills amended during the day, stored in /memory"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.9.0",
    "loguru>=0.7.0",
    "httpx>=0.24.0",
    "pydantic>=2.0",
]

[tool.uv]
package = false
