# Generated by test-lab — blind adversarial evaluation
"""test_gemini_submit - tests.

Purpose: Auto-generated module docstring. Review for accuracy.
Inputs/Outputs/Failures: See functions below.
"""

from __future__ import annotations

import json
import os
import subprocess
from pathlib import Path

import pytest


REPO_ROOT = Path(__file__).resolve().parents[3]
GEMINI_SUBMIT = REPO_ROOT / "skills/surf/scripts/gemini-submit.sh"


def test_gemini_submit_uses_requested_tab_id_when_upstream_omits_metadata(
    tmp_path: Path,
) -> None:
    request = tmp_path / "request.md"
    response = tmp_path / "response.md"
    meta = tmp_path / "response.meta.json"
    fake_run = tmp_path / "surf-run.sh"

    request.write_text("Reply with exactly: gemini smoke\n", encoding="utf-8")
    fake_run.write_text(
        """#!/usr/bin/env bash
set -euo pipefail
case "${1:-}" in
  focus.state)
    printf '{"focusedWindowId":1,"activeTabId":837361258}\\n'
    ;;
  gemini_tab)
    printf 'gemini smoke<<<GEMINI_DONE:test>>>\\n'
    ;;
  *)
    echo "unexpected surf command: $*" >&2
    exit 99
    ;;
esac
""",
        encoding="utf-8",
    )
    fake_run.chmod(0o755)

    env = os.environ.copy()
    env["SURF_RUN_SH"] = str(fake_run)
    proc = subprocess.run(
        [
            "bash",
            str(GEMINI_SUBMIT),
            "--input",
            str(request),
            "--output",
            str(response),
            "--meta-output",
            str(meta),
            "--sentinel",
            "<<<GEMINI_DONE:test>>>",
            "--tab-id",
            "837361258",
            "--no-activate",
            "--stable-polls",
            "0",
            "--timeout",
            "5",
        ],
        cwd=tmp_path,
        env=env,
        text=True,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        check=False,
    )

    assert proc.returncode == 0, proc.stderr
    payload = json.loads(meta.read_text(encoding="utf-8"))
    assert payload["status"] == "completed"
    assert payload["controlled_tab_id"] == "837361258"
    assert payload["controlled_tab_id_source"] == "requested_tab_id_fallback"
    assert payload["controlled_tab_id_mismatch"] is False


def test_gemini_submit_records_attachment_metadata(tmp_path: Path) -> None:
    request = tmp_path / "request.md"
    attachment = tmp_path / "review-bundle.md"
    response = tmp_path / "response.md"
    meta = tmp_path / "response.meta.json"
    fake_run = tmp_path / "surf-run.sh"

    request.write_text("Review ATTACHMENT_1.\n", encoding="utf-8")
    attachment.write_text("# Review bundle\n", encoding="utf-8")
    fake_run.write_text(
        f"""#!/usr/bin/env bash
set -euo pipefail
case "${{1:-}}" in
  focus.state)
    printf '{{"focusedWindowId":1,"activeTabId":837361258}}\\n'
    ;;
  gemini_tab)
    printf '%s\\n' "$*" > {tmp_path / "invocation.txt"}
    printf 'gemini smoke<<<GEMINI_DONE:test>>>\\n'
    echo 'ControlledTabID: 837361258' >&2
    echo 'Attachment: {{"attached":true,"path":{json.dumps(str(attachment.resolve()))},"name":"review-bundle.md","previewVisible":true}}' >&2
    ;;
  *)
    echo "unexpected surf command: $*" >&2
    exit 99
    ;;
esac
""",
        encoding="utf-8",
    )
    fake_run.chmod(0o755)

    env = os.environ.copy()
    env["SURF_RUN_SH"] = str(fake_run)
    proc = subprocess.run(
        [
            "bash",
            str(GEMINI_SUBMIT),
            "--input",
            str(request),
            "--output",
            str(response),
            "--meta-output",
            str(meta),
            "--sentinel",
            "<<<GEMINI_DONE:test>>>",
            "--tab-id",
            "837361258",
            "--no-activate",
            "--stable-polls",
            "0",
            "--timeout",
            "5",
            "--attach-file",
            str(attachment),
        ],
        cwd=tmp_path,
        env=env,
        text=True,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        check=False,
    )

    assert proc.returncode == 0, proc.stderr
    payload = json.loads(meta.read_text(encoding="utf-8"))
    assert payload["status"] == "completed"
    assert payload["attachment"] == {
        "attached": True,
        "path": str(attachment.resolve()),
        "name": "review-bundle.md",
        "previewVisible": True,
    }
    assert payload["attachment_missing"] is False
    assert f"--file {attachment.resolve()}" in (tmp_path / "invocation.txt").read_text(encoding="utf-8")


def test_gemini_submit_fails_when_attachment_metadata_is_missing(tmp_path: Path) -> None:
    request = tmp_path / "request.md"
    attachment = tmp_path / "review-bundle.md"
    response = tmp_path / "response.md"
    meta = tmp_path / "response.meta.json"
    fake_run = tmp_path / "surf-run.sh"

    request.write_text("Review ATTACHMENT_1.\n", encoding="utf-8")
    attachment.write_text("# Review bundle\n", encoding="utf-8")
    fake_run.write_text(
        """#!/usr/bin/env bash
set -euo pipefail
case "${1:-}" in
  focus.state)
    printf '{"focusedWindowId":1,"activeTabId":837361258}\\n'
    ;;
  gemini_tab)
    printf 'gemini smoke<<<GEMINI_DONE:test>>>\\n'
    echo 'ControlledTabID: 837361258' >&2
    ;;
  *)
    echo "unexpected surf command: $*" >&2
    exit 99
    ;;
esac
""",
        encoding="utf-8",
    )
    fake_run.chmod(0o755)

    env = os.environ.copy()
    env["SURF_RUN_SH"] = str(fake_run)
    proc = subprocess.run(
        [
            "bash",
            str(GEMINI_SUBMIT),
            "--input",
            str(request),
            "--output",
            str(response),
            "--meta-output",
            str(meta),
            "--sentinel",
            "<<<GEMINI_DONE:test>>>",
            "--tab-id",
            "837361258",
            "--no-activate",
            "--stable-polls",
            "0",
            "--timeout",
            "5",
            "--attach-file",
            str(attachment),
        ],
        cwd=tmp_path,
        env=env,
        text=True,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        check=False,
    )

    assert proc.returncode == 5
    payload = json.loads(meta.read_text(encoding="utf-8"))
    assert payload["status"] == "failed"
    assert payload["failure"] == "attachment_missing"
    assert payload["attachment_missing"] is True


def test_gemini_submit_accepts_fresh_stable_response_without_sentinel(
    tmp_path: Path,
) -> None:
    request = tmp_path / "request.md"
    response = tmp_path / "response.md"
    meta = tmp_path / "response.meta.json"
    fake_run = tmp_path / "surf-run.sh"

    request.write_text("Reply with exactly: gemini smoke\n", encoding="utf-8")
    fake_run.write_text(
        """#!/usr/bin/env bash
set -euo pipefail
case "${1:-}" in
  focus.state)
    printf '{"focusedWindowId":1,"activeTabId":837361258}\\n'
    ;;
  gemini_tab)
    printf 'gemini smoke\\n'
    echo 'ControlledTabID: 837361258' >&2
    echo 'Activated: false' >&2
    echo 'StableResponseWithoutSentinel: true' >&2
    ;;
  *)
    echo "unexpected surf command: $*" >&2
    exit 99
    ;;
esac
""",
        encoding="utf-8",
    )
    fake_run.chmod(0o755)

    env = os.environ.copy()
    env["SURF_RUN_SH"] = str(fake_run)
    proc = subprocess.run(
        [
            "bash",
            str(GEMINI_SUBMIT),
            "--input",
            str(request),
            "--output",
            str(response),
            "--meta-output",
            str(meta),
            "--sentinel",
            "<<<GEMINI_DONE:test>>>",
            "--tab-id",
            "837361258",
            "--no-activate",
            "--stable-polls",
            "2",
            "--timeout",
            "5",
        ],
        cwd=tmp_path,
        env=env,
        text=True,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        check=False,
    )

    assert proc.returncode == 0, proc.stderr
    assert response.read_text(encoding="utf-8") == "gemini smoke\n"
    payload = json.loads(meta.read_text(encoding="utf-8"))
    assert payload["status"] == "completed"
    assert payload["raw_contains_sentinel"] is False
    assert payload["stable_response_without_sentinel"] is True
    assert payload["proof_status"] == "response_proven_without_sentinel"


@pytest.mark.parametrize(
    "first_error",
    [
        "Error: Response timeout",
        "Error: Gemini did not accept submitted prompt: prompt remained in the composer after send",
    ],
)
def test_gemini_submit_retries_stalled_exact_tab_after_cooldown(
    tmp_path: Path,
    first_error: str,
) -> None:
    request = tmp_path / "request.md"
    response = tmp_path / "response.md"
    meta = tmp_path / "response.meta.json"
    fake_run = tmp_path / "surf-run.sh"
    attempts = tmp_path / "attempts"
    invocations = tmp_path / "invocations"

    request.write_text("Reply with exactly: gemini smoke\n", encoding="utf-8")
    fake_run.write_text(
        f"""#!/usr/bin/env bash
set -euo pipefail
printf '%s\\n' "$*" >> {invocations}
case "${{1:-}}" in
  focus.state)
    printf '{{"focusedWindowId":1,"activeTabId":837361258}}\\n'
    ;;
  gemini_tab)
    count=0
    [[ -f {attempts} ]] && count=$(cat {attempts})
    count=$((count + 1))
    printf '%s' "$count" > {attempts}
    if [[ "$count" -eq 1 ]]; then
      echo {first_error!r} >&2
      exit 1
    fi
    printf 'gemini smoke<<<GEMINI_DONE:test>>>\\n'
    ;;
  go)
    exit 0
    ;;
  *)
    echo "unexpected surf command: $*" >&2
    exit 99
    ;;
esac
""",
        encoding="utf-8",
    )
    fake_run.chmod(0o755)

    env = os.environ.copy()
    env["SURF_RUN_SH"] = str(fake_run)
    env["SURF_GEMINI_STALL_COOLDOWN_SECONDS"] = "0"
    proc = subprocess.run(
        [
            "bash",
            str(GEMINI_SUBMIT),
            "--input",
            str(request),
            "--output",
            str(response),
            "--meta-output",
            str(meta),
            "--sentinel",
            "<<<GEMINI_DONE:test>>>",
            "--tab-id",
            "837361258",
            "--no-activate",
            "--stable-polls",
            "0",
            "--timeout",
            "5",
        ],
        cwd=tmp_path,
        env=env,
        text=True,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        check=False,
    )

    assert proc.returncode == 0, proc.stderr
    payload = json.loads(meta.read_text(encoding="utf-8"))
    assert payload["status"] == "completed"
    assert payload["stall_retry_count"] == 1
    assert payload["stall_retry_attempts"] == 1
    assert payload["stall_cooldown_seconds"] == 0
    assert attempts.read_text(encoding="utf-8") == "2"
    assert "go https://gemini.google.com/app --tab-id 837361258 --json" in invocations.read_text(
        encoding="utf-8"
    )


def test_gemini_submit_truncates_trailing_content_after_the_sentinel(tmp_path: Path) -> None:
    """agent-skills#1025: Gemini appends an echo after the marker.

    The attributable answer sits before the sentinel, so the lane must truncate
    there and keep the discarded tail as evidence, not fail the whole seat.
    """
    request = tmp_path / "request.md"
    response = tmp_path / "response.md"
    meta = tmp_path / "response.meta.json"
    fake_run = tmp_path / "surf-run.sh"

    request.write_text("Reply with exactly: gemini smoke\n", encoding="utf-8")
    fake_run.write_text(
        """#!/usr/bin/env bash
set -euo pipefail
case "${1:-}" in
  focus.state)
    printf '{"focusedWindowId":1,"activeTabId":837361258}\\n'
    ;;
  gemini_tab)
    printf 'gemini smoke<<<GEMINI_DONE:test>>>\\ntrailing echo the model appended\\n'
    ;;
  *)
    echo "unexpected surf command: $*" >&2
    exit 99
    ;;
esac
""",
        encoding="utf-8",
    )
    fake_run.chmod(0o755)

    env = os.environ.copy()
    env["SURF_RUN_SH"] = str(fake_run)
    proc = subprocess.run(
        [
            "bash", str(GEMINI_SUBMIT),
            "--input", str(request),
            "--output", str(response),
            "--meta-output", str(meta),
            "--sentinel", "<<<GEMINI_DONE:test>>>",
            "--tab-id", "837361258",
            "--no-activate",
            "--stable-polls", "0",
            "--timeout", "5",
        ],
        cwd=tmp_path, env=env, text=True,
        stdout=subprocess.PIPE, stderr=subprocess.PIPE, check=False,
    )

    assert proc.returncode == 0, proc.stderr
    clean = response.read_text(encoding="utf-8")
    assert clean.strip() == "gemini smoke"
    assert "<<<GEMINI_DONE:test>>>" not in clean
    assert "trailing echo" not in clean
    trailing = response.with_name(response.name + ".trailing.txt")
    assert trailing.is_file()
    assert "trailing echo the model appended" in trailing.read_text(encoding="utf-8")
    payload = json.loads(meta.read_text(encoding="utf-8"))
    assert payload["status"] == "completed"
    assert payload["proof_status"] == "response_proven"
