# BATTLE runtime atlas prompt (PRODUCTION — THIS BECOMES {{SPRITE_ID}}.png)
# Requires approved contact sheet and/or character reference as upload.
# Target output: exactly 512x896 transparent PNG, 8 cols x 14 rows, 64x64 cells.

Use the uploaded image as the ONLY reference.

Create a production-ready transparent PixiJS v8 sprite atlas for:

Character name: {{DISPLAY_NAME}}
Variant id: {{SPRITE_ID}}
Cell size: 64x64
Atlas grid: 8 columns x 14 rows
Atlas size: 512x896

Preserve these reference traits:
{{TRAIT_BULLETS}}

Runtime requirements:
- transparent PNG
- frames only
- no labels
- no text
- no side panel
- no checkerboard background
- no visible grid
- no dark grid lines
- strict equal-size cells
- every cell exactly 64x64
- character always faces right
- consistent scale
- consistent baseline
- no frame overlap
- no effect crossing cell boundaries
- empty unused cells fully transparent

Rows:
0 idle 4 frames
1 walk 6 frames
2 run 8 frames
3 research 6 frames
4 payload 6 frames
5 mutate 6 frames
6 handoff 6 frames
7 spawn 8 frames
8 blocked 6 frames
9 hit 3 frames
10 killed 8 frames
11 victory 8 frames
12 promoted 8 frames
13 fastest_crash 8 frames

For rows with fewer than 8 frames, leave remaining cells transparent.

Style:
Original 16-bit side-view pixel art, early-console side-scroller, crisp hard pixel edges, nearest-neighbor scaling, limited palette, readable silhouette.

Do not copy franchise logos, faction marks, copyrighted symbols, labels, or commercial sprites.

NEGATIVE PROMPT (use if model supports it):
{{NEGATIVE_PROMPT}}
