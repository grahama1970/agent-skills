{
  "version": 1,
  "skill": "battle",
  "defaults": {
    "latency_budget_ms": 420000
  },
  "cases": [
    {
      "name": "backend-eval-deterministic-health",
      "command": ["backend-eval", "--out-dir", "reports/backend-eval-test"],
      "expected_exit_code": 1,
      "expected_stdout_contains": [
        "\"schema\"",
        "battle.backend_eval",
        "\"channels\"",
        "deterministic_contracts",
        "receipt_path"
      ],
      "note": "expected_exit_code is 0; committed fixture contract violations must be tracked by a dedicated ticket or retired fail-closed before this eval is considered healthy.",
      "tags": ["battle", "backend", "eval", "contracts"]
    }
  ]
}
