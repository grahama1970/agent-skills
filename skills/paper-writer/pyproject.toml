[project]
name = "paper-writer"
version = "0.1.0"
description = "Interview-driven paper generation orchestrator"
requires-python = ">=3.10"
dependencies = [
    "typer",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
