[project]
name = "pi-skill-mine-transcripts"
version = "0.1.0"
description = "Runtime package for the mine-transcripts skill"
requires-python = ">=3.11,<3.13"
dependencies = [
    "httpx>=0.27.0,<0.29.0",
    "loguru>=0.7.0,<0.7.3",
    "pytest>=8.0.0,<9.0.0",
    "typer>=0.9.0",
]

[tool.uv]
package = false
