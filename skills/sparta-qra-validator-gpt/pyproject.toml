[project]
name = "pi-skill-sparta-qra-validator-gpt"
version = "0.1.0"
description = "Runtime package for the sparta-qra-validator-gpt skill"
requires-python = ">=3.11,<3.13"
dependencies = [
    "duckdb",
    "loguru>=0.7.0,<0.7.3",
    "rich>=13.0.0",
    "typer>=0.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
