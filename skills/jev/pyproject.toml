[project]
name = "jev-skill"
version = "0.1.0"
description = "Typed judgment calls via TypeSafe Jev with house egress gate and fail-closed abstention"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.24.0",
    "typer>=0.9.0",
    "loguru>=0.7.0",
    "pydantic>=2.0",
]
