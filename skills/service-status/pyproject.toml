[project]
name = "pi-skill-service-status"
version = "0.1.0"
description = "Runtime package for the service-status skill"
requires-python = ">=3.11,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
