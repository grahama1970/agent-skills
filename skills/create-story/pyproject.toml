[project]
name = "create-story"
version = "0.1.0"
description = "Creative writing orchestrator for Horus persona"
requires-python = ">=3.11,<3.13"
dependencies = [
    "typer>=0.9.0",
    "loguru>=0.7.0,<0.7.3",
    "rich>=13.0.0",
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
]
