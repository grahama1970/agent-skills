# Full-deck /ask review (concurrent image+json checks -> reviewer per slide)

## s1-01.png|SpartaAI_CyberSummitv_v3#19

## Position
VERDICT: FAIL. The generated slide is close in skeleton (band + big title + subtitle + right-side emblem + footer mark) but fails on both a structural bbox collision and an art-register mismatch. The single biggest gap is **structural**: the cover-mark bbox overlaps the title bbox, whereas the real page keeps the emblem entirely below the title band.

## Evidence
**Check A (visual):**
- Band: matched well — dark teal header band, white sans title, similar height and left inset. Good.
- Type scale: real title is a large centered display word spanning the full width; generated title is smaller and pushed left-of-center to make room for the emblem. Density feel differs.
- Art register: the generated helmet is a flat, saturated bright cyan; the house shield is a muted, desaturated teal with fine linework. Register clash is the most visible fidelity miss.
- Emblem position: generated mark sits mid-slide beside the title; real emblem sits bottom-right, below the title zone.
- Footer: both have a bottom-left mark/legal strip and page number — acceptable match.

**Check B (structural):**
- Collision: `title` (x 0.08–0.92, y 0.30–0.48) intersects `cover-mark` (x 0.66–0.92, y 0.3

## s10-10.png|SpartaAI_CyberSummitv_v3#30

## Position
VERDICT: FAIL. The generated slide neither passes visual fidelity (truncated title, wrong art register) nor structural checks (multiple bbox collisions in the bottom band). The single biggest gap is **structural**: thumb-0 and thumb-1 (y 0.71, h 0.21 → bottom edge 0.92) collide with both the visual-caption (y 0.83–0.88) and the qualifier footer (y 0.845–0.92), stacking three elements into the same lower region — visible on the left image as thumbnails jammed against the footer strip with a caption fragment ("deta…") clipped between them.

## Evidence
**Check A (visual):**
- Title is truncated mid-word ("…Mapping Badges (de"), a clear render failure; the real page has a short, complete 3-word title.
- Art register mismatch: generated uses dense dark-terminal screenshots; the real house page uses light, boxed product panels with dashed connectors and per-panel blue captions.
- Composition mismatch: real page is a three-column equal-panel layout with captions under each; generated is one dominant image + left callout + two crowded thumbs — a different template family.
- Band and footer treatment (teal header, footer strip, house-mark, page number) do match the house style;

## s11-11.png|SpartaAI_CyberSummitv_v3#30

## Position
VERDICT: FAIL. The generated slide is a passable house-style cousin of the real page but fails on structural grounds: the lower third of the layout is a collision zone where both thumbnails overlap the visual-caption and the footer qualifier. The single biggest gap is **structural** — thumb-0/thumb-1 (y 0.71–0.92) intrude into the caption band (y 0.83–0.88) and footer band (y 0.845–0.92).

## Evidence
**Check A (visual):**
- Band and type register match: teal title band, white body, house mark bottom-left, page number bottom-right — consistent with the real page.
- Title is truncated mid-phrase ("…with Sources And") — a visible fidelity defect the real page never shows.
- Art register mismatch: generated uses dark-terminal screenshots; the real page uses light UI/diagram panels in three balanced columns. Generated is left-heavy (callout + one dominant image) vs. the real page's symmetric 3-up rhythm.
- On the render, the caption text is visibly crushed between the two thumbnails — confirming the JSON collision.

**Check B (structural):**
- thumb-0 (0.30–0.62, 0.71–0.92) ∩ visual-caption (0.30–0.96, 0.83–0.88): overlap.
- thumb-1 (0.64–0.96, 0.71–0.92) ∩ visual-caption a

## s12-12.png|ReqML_GE_Presentation#54

## Position
VERDICT: FAIL. The generated slide is a credible divider archetype, but Check B reveals a region violation (small mark relocated from top-right to bottom-left), an oversized heading block, and a minor bbox collision. Critically, Check A cannot be trusted: the "nearest real" image on the right of the composite is a dense content slide ("Rales (Provers Option #2)", pg 58) — it does not match the JSON record (divider "Where does this Lead?", slide_index 54, pdf_page 50), so the visual comparison is against the wrong page.

**Biggest gap: structural** — the house-mark badge is at (0.02, 0.875) bottom-left, while the real record's corresponding small picture sits at (0.93, 0.009) top-right; compounded by the heading block being ~78% taller than the real one.

## Evidence
- **Title**: gen (0.02, 0.02, 0.76, 0.08) vs real (0.009, 0.013, 0.85, 0.075) — good match; width slightly narrow (−0.09).
- **Divider heading**: gen (0.08, 0.34, 0.84, 0.24) vs real (0.0, 0.375, 1.0, 0.135). Height delta +0.105 (0.24 vs 0.135); real spans full width, generated is inset. Word density: gen "Where This Goes" = 3 words vs real 5 — small deficit against total_words = 9.
- **Divider mark**: gen (

## s13-13.png|SpartaAI_CyberSummitv_v3#34

## Position
VERDICT: **FAIL**. The single biggest gap is **structural**: a massive density mismatch — the generated slide is a content-heavy roadmap (title, chevron summary, multi-node diagram, ~35 words) while its "nearest" real page is a 4-word section divider ("Open Discussion", 2 text blocks, zero diagram). This is a retrieval/pairing failure more than a rendering failure; the generated slide cannot be validated against this record.

## Evidence
**Check A (visual):**
- Band: header band color/height plausibly matches house style, but the generated title wraps to two lines and its second line spills below the band edge; the real page keeps a single-line title inside the band.
- Clipping: the yellow "deployment remain explicit closure gates" callout runs off the right slide edge, and a shield/check icon near "QRA reasoning" is cut at its bounding region — visible render defects.
- Register: dotted-line flag/shield diagram is a reasonable house metaphor register, but density (~35 words, 5 nodes) is far above the real page's 4 words.

**Check B (structural):**
- Density delta: generated ≈35 words across 3 text-bearing elements vs. real `total_words: 4`, `text_block_count: 2`. Delta

## s14-14.png|SpartaAI_SBIR#3

## Position
VERDICT: PASS (marginal). The generated slide reads convincingly as a house page — correct teal title band, chevron glyph register, left-text/right-visual split, and footer treatment. The single biggest gap is **structural**: the house-mark badge sits bottom-left (x 0.02, y 0.875) while the house record places its picture badge top-right (x 0.9347, y 0.0234) — a region violation that also creates a minor bbox collision with the qualifier footer.

## Evidence
**Check A (visual):**
- Title band: color, height (~8% vs 7.5%), and type scale match the real page closely.
- Chevron glyph and body type register match the house TOC's chevron list.
- Density is lower left-of-center (2 chevron blocks vs. the real page's dense 25-word list), but acceptable for a content slide.
- Right-side visual placement roughly mirrors the real Venn shape zone (gen x 0.50 vs real shape x 0.5946).

**Check B (structural):**
- Title bbox close: gen (0.02, 0.02, 0.76×0.08) vs real (0.0092, 0.0129, 0.85×0.0749) — width undershoots by ~0.09.
- Region violation: badge at bottom-left vs house's top-right picture slot.
- Bbox collision: house-mark (x 0.02–0.07, y 0.875–0.965) overlaps qualifier (x 0.06–

## s15-15.png|ACERT_Darpa_PI_Meeting_FtWorth#55

## Position
VERDICT: FAIL. The single biggest gap is **structural**: the generated `divider-heading` bbox both deviates from the real full-bleed band (w 0.84 vs 1.0, h 0.24 vs 0.157) and collides with `divider-mark`, and the record's header picture block is missing entirely. Note also that Check A is compromised — the right-hand image ("Simplistic", p.64) is not the record described in the JSON ("Open Discussion", pdf_page 46), so the visual comparison target is wrong.

## Evidence
**Check A (image):** The generated slide itself reads plausibly as a house divider — dark teal header band with white title, large centered display heading, low density, cyan helmet mark lower-right, footer logo + confidentiality strip. However, the paired "real" page is a dense content slide (diagram, robot art, red question text), so no meaningful fidelity comparison against the true divider template is possible from this composite.

**Check B (json):**
- **Collision:** `divider-heading` spans x[0.08–0.92], y[0.34–0.58]; `divider-mark` spans x[0.72–0.92], y[0.54–0.88] → overlap region x[0.72–0.92] × y[0.54–0.58]. The real record has clean separation (heading ends y≈0.532; shape starts y=0.5406).
- **Re

## s2-02.png|ReqML_GE_Presentation#54

## Position
VERDICT: FAIL. The generated slide reads plausibly as a house slide visually (dark teal header band, footer disclaimer strip, page-corner styling all match the register), but it fails structurally: both image badges are placed in regions the real record never uses, and the composite's "nearest real page" does not match the JSON record it's paired with, which undermines Check A.

**Single biggest gap: structural** — the `house-mark` badge sits bottom-left (x=0.02, y=0.875) while the house convention per the record puts the small mark top-right (x=0.932, y=0.009); similarly `toc-mark` sits mid-right (y=0.28) versus the real large picture anchored bottom-right (x=0.759, y=0.550).

**Mechanical fixes (max 3):**
1. Move `house-mark` to top-right: bbox → {x: 0.93, y: 0.01, w: 0.042, h: 0.074}.
2. Move/resize `toc-mark` to the bottom-right picture zone: bbox → {x: 0.76, y: 0.55, w: 0.246, h: 0.405}.
3. Widen `title` to match house scale: w 0.76 → 0.85, x → 0.01.

## Evidence
- **Check A (visual):** Header band color/height, sans type, footer disclaimer + logo, and page number placement on the left slide are consistent with the right page's chrome. Density is far lower on the l

## s3-03.png|ReqML_GE_Presentation#54

## Position
VERDICT: FAIL. The generated slide credibly reproduces the house *register* (teal band, top-right badge, minimal statement slide, footer mark), but the structural comparison against the real record shows the two anchor blocks are materially off: the hero statement text is displaced upward and over-tall, and the bottom-right image is ~45% under-sized. Biggest gap is **structural**: the title block sits at y=0.22 (h=0.26) versus the record's full-width statement at y=0.375 (h=0.135) — a ~0.15 vertical offset plus 2× height inflation.

Mechanical fixes (≤3):
1. Retarget `title` bbox to x=0.0, y=0.375, w=1.0, h=0.135 (full-bleed centered statement, matching real block 2).
2. Enlarge `thesis-mark` to x=0.759, y=0.550, w=0.246, h=0.405 to match the real bottom-right picture footprint.
3. Add a band-title text block at (0.009, 0.013, 0.85, 0.075) to the structure — it is visible in the render but absent from the JSON, so structure and pixels disagree.

## Evidence
- **Check A (visual):** Left slide matches house grammar — teal header band, circular badge upper-right, sparse statement layout, cyan helmet mark, footer logo + confidentiality line. Type scale and density register 

## s4-04.png|ReqML_GE_Presentation#63

## Position
VERDICT: FAIL. The generated slide is a plausible house-style divider (band, type, sparse register all look on-brand), but it fails structurally against the cited real record — and there is a compositing inconsistency: the RIGHT panel shows a dense "ReqAo Workflow" diagram slide, while the JSON record describes a sparse 2-word "Thank You!" divider. The check as presented cannot both be true. Judging against the JSON (the canonical record), the biggest gap is **structural**: the divider mark sits in the lower-right quadrant (x 0.72–0.92, y 0.54–0.88) whereas the real house pattern centers the mark (x 0.357–0.645, y 0.217–0.783), and the generated layout adds a large heading block the real record does not have.

## Evidence
- **Check A (visual):** Header band, dark-teal palette, white title type, footer legalese line, and page-number placement all match the house register. Density delta vs. the *displayed* right image is enormous (divider vs. ~30-element workflow diagram), but that comparison is invalid — wrong neighbor rendered.
- **Check B (structural):**
  - Title: generated (0.02, 0.02, 0.76, 0.08) ≈ real (0.009, 0.013, 0.85, 0.075). Good match, slightly narrow.
  - M

## s5-05.png|SpartaAI_SBIR#3

## Position
VERDICT: FAIL. The single biggest gap is **visual — art register**. The generated slide drops a dense, dark terminal/HUD screenshot (mono type, red/green status text, near-black field) into a house style that is flat, light, vector-line, teal-on-white. The band, badge placement, and chevron vocabulary are on-model; the scene art is not.

## Evidence
**Check A (image):**
- Band: full-width teal title bar with white sans title and a right-edge glyph badge — matches the real page closely. PASS on band.
- Type scale: title and chevron body sizes read within house range. PASS.
- Art register: FAIL. Real page uses flat vector venn/icons and a light logo strip; generated uses a screenshot-style dark console at ~50% slide area, spiking perceived density and contrast far outside the corpus.
- Density: generated has 2 chevron lines vs. the real page's ~11 list entries — text sparse, image heavy; the balance is inverted relative to the house pattern.

**Check B (json):**
- Collision: `scene-art` (y 0.40–0.90) overlaps `qualifier` footer (y 0.845–0.92) across x 0.14–0.86 — a real bbox intersection.
- Minor collision: `house-mark` (x 0.02–0.07) grazes `qualifier` (x starts 0.06).
- 

## s6-06.png|ReqML_GE_Presentation#63

## Position
VERDICT: FAIL — but with a caveat: the failure is driven by an evidence mismatch. The JSON "real record" describes a sparse 2-word divider ("Thank You!", one centered shape, one small picture), which the generated slide actually resembles well. The composite image, however, shows a dense "ReqAo Workflow" diagram page as the "nearest real," which the generated divider does not resemble at all. Judged strictly against what I SEE (Check A), fidelity fails on density and art register; judged against the JSON (Check B), the structure is close but has a bbox collision and a misplaced house mark. The single biggest gap is **structural**: the generated house-mark badge sits bottom-left (x 0.02, y 0.875) while the house record places its picture top-right (x 0.939, y 0.024) — plus the retrieval inconsistency itself.

Mechanical fixes (max 3):
1. Move `house-mark` to top-right: bbox ≈ {x: 0.94, y: 0.024, w: 0.04, h: 0.07} to match the real picture block.
2. Resolve the `divider-heading` / `divider-mark` overlap: heading spans y 0.34–0.58 and x 0.08–0.92; mark starts y 0.54, x 0.72 — shrink heading to h 0.18 (ends y 0.52) or drop mark to y 0.60.
3. Re-center `divider-mark` toward 

## s7-07.png|SpartaAI_SBIR#44

## Position
VERDICT: **FAIL**. The generated slide is a clean title-band + two-chevron + single-hero-image layout; the real page is a dense, multi-lane pipeline diagram (ingest → extract → ontology → build case → measure/explore) built from ~12+ overlapping column shapes, callouts, and logo strips. The single biggest gap is **structural**: the generated JSON models one monolithic `scene-art` image where the real record decomposes the canvas into full-height lane shapes (e.g., `x:0.0076 w:0.1925 h:0.8003`, `x:0.6351 w:0.1312 h:0.8037`) plus embedded text/shape clusters. Visual fidelity fails secondarily on density and art register (dark neon-diagram vs. light annotated schematic on white).

## Evidence
- **Check A (visual):** Real page has no teal title band spanning the full width as a filled bar in the same register — its title sits on a band but the body is white with fine-line vectors, small-scale labels, and vendor logo footer (GE, Honeywell, etc.). Generated slide uses a dark, high-contrast promotional graphic centered at ~72% width — wrong art register and far lower element density (~6 visible elements vs. dozens).
- **Check B (structural):**
  - Element count delta: 6 genera

## s8-08.png|ReqML_GE_Presentation#63

## Position
**VERDICT: FAIL.** The single biggest gap is **structural**: the generated layout is being judged against a real record it does not match — the JSON is a near-empty "Thank You!" slide (2 words, 3 blocks), while the generated slide is a content slide with title + 2 chevrons + hero screenshot. Visually the generated slide is actually credible house style; the structural comparison is what breaks.

## Evidence
**Check A (image):** The left slide reads plausibly on-register — teal title band with white sans title, chevron-glyph bullets in body text, dark product screenshot right, footer confidentiality strip, house mark bottom-left. However, the "nearest real" page shown right ("ReqAo Workflow") is a dense multi-column diagram slide with badge **top-right**, footer mark **bottom-left inline with legalese**, and far higher visual density. Composition similarity between left and right is low.

**Check B (json):**
- **Badge region violation:** generated `house-mark` at (0.02, 0.875) bottom-left vs real picture at (0.939, 0.024) top-right — opposite corners.
- **Density delta:** real record has `total_words: 2`, one text block; generated has 3 text elements (title + 2 chevrons,

## s9-09.png|ReqML_GE_Presentation#63

## Position
VERDICT: PASS — with a caveat. The generated slide (left) is a credible house-style divider: correct dark teal title band, correct type scale, footer with logo + legal line, and a flat cyan mascot mark in the brand register. The structural comparison against the JSON record is reasonable, but note the composite's right-hand image ("ReqAo Workflow", a dense content slide) does **not** match the JSON record it was paired with ("Thank You!", a sparse 2-word divider at slide_index 63). The visual check is therefore against the wrong reference; Check B is the reliable check here.

**Biggest gap: structural** — badge region violation. The house pattern (per the record) places its picture badge top-right (x=0.939, y=0.024, ~0.04×0.07), while the generated slide places its marks bottom-right (0.72, 0.54, 0.2×0.34) and bottom-left (0.02, 0.875), and the divider-mark is ~14× the house badge area.

**Mechanical fixes (≤3):**
1. Move/resize house-mark to top-right: bbox → {x: 0.94, y: 0.02, w: 0.04, h: 0.07}, matching the real record's picture block.
2. Shrink divider-mark or center it as the record's shape does (real shape: x=0.357, y=0.217, w=0.287, h=0.566); e.g., bbox → {x: 0.3
