# pitchdeck command reference

Hand-written conceptual docs go above/below the markers.

<!-- AUTOGEN:COMMANDS BEGIN -->

_Generated by scripts/generate_docs.py — do not edit between markers._

### Plan

| Command | Purpose | Options |
|---|---|---|
| `scaffold` | Copy a source-controlled deck bundle template into a project. | `--profile` `--output-dir` `--force` |
| `plan` | Extract candidate claims/assets and generate a draft public deck manifest. | `--source-manifest` `--output-dir` `--max-slides` `--json-output` |
| `doctor` | Check Python dependencies and optional Linux render tools without prompting. | `--json-output` |
| `version` | Print the skill version. |  |

### Review

| Command | Purpose | Options |
|---|---|---|
| `claim-decide` | Decide one candidate claim through full validation; appends a replayable audit line. | `--bundle-dir` `--output-dir` `--claim-id` `--decision` `--decided-by` `--qualifier` `--batch` `--deck-name` `--json-output` |
| `simulate` | Dry-run a mutation through the REAL pipeline: JSON {would_pass, gate_codes, error, diff}; zero writes. | `--bundle-dir` `--slide-id` `--field` `--value` `--op` `--target-order` `--deck-name` |
| `drift` | Living-deck drift check: report changed sources + affected claims/slides; --update refreshes the snapshot. | `--bundle-dir` `--update` `--deck-name` |
| `undo` | Restore the previous committed state — validated BEFORE commit; refuses governance/out-of-band. | `--bundle-dir` `--output-dir` `--deck-name` `--json-output` |

### Design

| Command | Purpose | Options |
|---|---|---|
| `image-variations` | Compile a theme-aware brief and fan out N image variations (plan always; live with --execute). | `--bundle-dir` `--slide-id` `--output-dir` `--count` `--execute` `--deck-name` |
| `compile-document` | Compile the bundle into the canonical whole-deck document (pitchdeck.deck_document.v1). | `--bundle-dir` `--output` `--deck-name` |
| `render-document` | Render a canonical deck document to house-native self-contained HTML. | `--document` `--output` `--asset-base` `--theme-template` `--preview` |
| `record-note` | Record narration (RealtimeSTT mic capture, faster-whisper local transcription) into slide notes. | `--bundle-dir` `--output-dir` `--slide-id` `--timeout-seconds` `--deck-name` |
| `record-transcript` | Record one utterance (RealtimeSTT mic, faster-whisper local) and print the transcript JSON. | `--timeout-seconds` |

### Present / Publish

| Command | Purpose | Options |
|---|---|---|
| `build` | Validate manifests and compile an editable 16:9 PPTX. | `--deck` `--claim-ledger` `--source-manifest` `--asset-manifest` `--output` `--draft-watermark` `--require-approved-claims` `--json-output` |
| `emit-ui` | Validate the bundle and emit deck.data.json for the React deck renderer in ui/. | `--bundle-dir` `--output-dir` `--deck-name` `--require-approved-claims` `--json-output` |
| `emit-html` | Self-contained interactive HTML deck (assets inlined, no external resources). | `--bundle-dir` `--output` `--deck-name` `--max-width` `--quality` `--json-output` |
| `emit-md` | One-way Marp Markdown export; edits belong in the YAML manifests, not deck.md. | `--bundle-dir` `--output-dir` `--deck-name` `--json-output` |

### Maintain

| Command | Purpose | Options |
|---|---|---|
| `apply-edit` | Edit one slide text field; re-validates the full bundle before writing anything. | `--bundle-dir` `--output-dir` `--slide-id` `--field` `--value` `--base-revision` `--deck-name` `--json-output` |

### Ungrouped

| Command | Purpose | Options |
|---|---|---|
| `analyze-style` | Analyze a reference deck into validated pitchdeck.style_reference.v1 data. | `--pptx` `--output-dir` `--design-system` `--render` |
| `approve-rendering` | Approve one assertion rendering (human provenance required). | `--outline` `--module` `--index` `--approved-by` |
| `asset-add` | Copy a file into the bundle, register it, and bind it as the slide's visual. | `--bundle-dir` `--output-dir` `--slide-id` `--file` `--alt` `--deck-name` `--json-output` |
| `asset-clear` | Detach a slide's visual (the asset stays registered in the manifest). | `--bundle-dir` `--output-dir` `--slide-id` `--deck-name` `--json-output` |
| `bindings-migrate` | Auto-classify content bindings: exact claim text -> claim_quote, qualifier text -> qualifier. | `--bundle-dir` `--deck-name` `--triage-rest` `--json-output` |
| `deck-op` | Slide add/duplicate/delete/reorder; full bundle validation before writing. | `--bundle-dir` `--output-dir` `--op` `--slide-id` `--target-order` `--base-revision` `--deck-name` `--json-output` |
| `design-lint` | Deterministic DESIGN_* lint over a canonical document (exit 1 on findings). | `--document` |
| `emit-document-pptx` | Emit a canonical document as NATIVE editable PPTX (nested groups, shapes, connectors, runs). | `--document` `--output` `--asset-base` `--theme-template` |
| `emit-handout` | Two-slides-per-page A4 speaker handout with wrapped notes (Pillow, server-side). | `--bundle-dir` `--render-dir` `--output` `--deck-name` `--json-output` |
| `materialize-outline` | Materialize an APPROVED outline into an intent-carrying deck document (deterministic). | `--outline` `--context` `--bundle-dir` `--output` `--preview-candidates` |
| `memory-sync` | Store the emitted deck summary in /memory (ArangoDB) for /memory recall. | `--deck-data` `--verify-recall` `--json-output` |
| `outline` | Two-stage planning: draft (and optionally approve) a claim-routed narrative outline. | `--context` `--bundle-dir` `--profile` `--output` `--approve-by` |
| `propose-renderings` | Propose tightened assertion RENDERINGS (candidates only; human approval promotes). | `--outline` `--bundle-dir` `--proposals` `--max-words` |
| `render` | Render a PPTX to PDF, per-slide PNGs, and a contact sheet on Linux. | `--pptx` `--output-dir` `--dpi` `--json-output` |
| `source-edit` | Replace the deck manifest from edited YAML; full validation before writing. | `--bundle-dir` `--output-dir` `--source-file` `--base-revision` `--deck-name` `--json-output` |
| `verify` | Verify the bundle contract and, optionally, the generated PPTX. | `--bundle-dir` `--pptx` `--deck-name` `--require-approved-claims` `--json-output` |
| `visual-sync` | Index slide images into Qdrant (text_mm+image_mm) with memory pointer docs. | `--deck-data` `--images-dir` `--json-output` |

<!-- AUTOGEN:COMMANDS END -->
