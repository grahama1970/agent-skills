[project]
name = "governance-skill"
version = "0.1.0"
description = "Deterministic pre-plan governance gate"
requires-python = ">=3.10,<3.13"
dependencies = [
    "typer>=0.12",
    "python-dotenv>=1.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[tool.uv]
package = false
