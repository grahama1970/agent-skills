# Persona Dream roadmap (r2 — CONTROLLING, webgpt-converged PLAN_STABLE 2026-07-19)

Convergence: 2-round webgpt iteration (round 1 PLAN_REVISED applied in full;
round 2 PLAN_STABLE, gate ROADMAP_R2_VALIDITY_FIRST_ORDERING closed).
Receipts: review-bundles/roadmap_iteration_r{1,2}_20260719-assess-response.md.

Validity-first ordering: evidence-authority coherence, measurement validity,
v2-derived cognition, and the last unproven acceptance item come BEFORE the
confirmatory pilot; the public reproducible execution path comes BEFORE any
repeatability claim. Current truth: CURRENT_STATE.md.

## P0 — finish and validate the founding boundary before launching comparison

1. HUMAN: subjective review of the exact active successor provider return
   (video sha256:59b9ff31…, request 97688ec5…), human-authored receipt,
   hash-bound. Near-zero effort; may proceed in parallel with items 2-6.
2. Active-authority convergence: CURRENT_STATE selects protocol v2 as the
   frozen pilot authority (v1 superseded pre-run) and pins ONE non-superseded
   evidence lineage from accepted observation v2 through interpretations,
   persistence, and the current phase-16 receipt — no cross-version mixing.
3. Routing semantics + montage-equivalence debt (issues/428): role hierarchy
   preserved through Tau nodes; montage verdict-equivalence calibrated on the
   reviewer_calibration_v4 frame set; ArcFace authority enforced (not
   advisory) at identity call sites.
4. GMO deployment convergence: HUMAN merges GMO mainline, or we pin the exact
   deployed GMO commit used by all new canonical writes (recorded in run
   manifests) until the merge happens.
5. Versioned phases 13-16 rerun from accepted observation packet v2 as
   revision bundles (one dream-event node; derived evidence superseded, never
   duplicated), then regenerate CURRENT_STATE from that lineage.
6. Voice lane (moved up — the sole unproven machine-facing acceptance item):
   native Chatterbox render of an Embry post-dream performance
   (affective-performance contract), dual-route text/audio evaluation, plus a
   deliberately readable visible-speaker canary through the lip-sync lane. No
   paid provider call implied.
7. Pilot: finalize frozen R1/R2 selection addendum + producer-blinding
   receipts, execute the frozen C-vs-F pilot under protocol v2, operator M5
   blind read, publish positive/null/invalid under the frozen rules.

## P1 — make the proven lane repeatable

8. Tracked temporal identity certification for the full clip (face tracking,
   pose-aware thresholds, per-beat expectations) before the next full video
   run.
9. Clean-room reproducibility envelope: freeze Tau + GMO commits, model and
   threshold policies, dependency + media manifests, artifact-hydration
   sources, evaluation policy; prove the public command path from a fresh
   checkout with no source edits.
10. Manifest-driven dream CLI: run.sh dream
    start/observe/interpret/promote/evaluate/resume — idempotent,
    predecessor-discovering, one transition receipt per command. Precondition
    for any zero-intervention claim.
11. GMO server commit-lifecycle endpoints (staging/commit-open) for safe
    resumable multi-run operation.
12. Second serial SAME-persona full run under the frozen policy with an
    immutable intervention ledger (every retry, threshold change, manual
    selection, prompt change, source edit, or service fallback recorded; any
    intervention disqualifies "zero manual repair" but is kept as exploratory
    evidence). Simplest serial generation path — no concurrency.
13. Second-persona run only after item 12 passes (repeatability and
    generality are distinct claims).

## P2 — extend demonstrated capability

14. Persist Watch observations as multimodal Qdrant points; test cross-modal
    retrieval ("what did the dream look like").
15. Full A-G ablation only if the cheap pilot supplies the predeclared
    signal.
16. Concurrent two-wave frame generation, autonomous dream scheduling, React
    Flow canvas, deeper state-promotion policy — only after repeatability
    exists.
