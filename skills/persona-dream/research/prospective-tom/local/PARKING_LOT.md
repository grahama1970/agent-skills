# Parking Lot

- Build the deterministic social-world simulator after Gate 0 lineage fixtures
  fail closed.
- Add Tau text-call adapters only after the sealed commitment file contract is
  mechanically stable.
- Add Brier/log-loss scoring after immutable reveal records exist.
- Add reliability surface `R(k, epsilon, lambda)` after one trial can seal and
  reveal without human content judgment.
