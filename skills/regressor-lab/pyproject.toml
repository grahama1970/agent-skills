[project]
name = "regressor-lab"
version = "0.1.0"
requires-python = ">=3.11,<3.13"
dependencies = [
    "loguru>=0.7,<0.7.3",
    "PySide6",
    "typer>=0.9",
]

[project.optional-dependencies]
tuning = ["optuna>=3.0"]
gui = ["PySide6>=6.6.0"]
