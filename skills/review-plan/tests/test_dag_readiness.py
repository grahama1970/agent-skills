# Generated by test-lab — blind adversarial evaluation
"""test_dag_readiness - tests.

Purpose: Auto-generated module docstring. Review for accuracy.
Inputs/Outputs/Failures: See functions below.
"""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from dag_readiness import DagReadinessReport, validate_dag_readiness, write_dag_readiness_report

ROOT = Path(__file__).resolve().parents[3]


def test_dag_readiness_passes_registry_and_graph() -> None:
    report = validate_dag_readiness(
        dag_path=ROOT / "plans/review-design-subagent-orchestration.dag.json",
        registry_path=ROOT / "workers-registry.json",
        project_root=ROOT,
        run_phart=False,
        check_compile=False,
    )
    assert report.missing_agents == []
    assert report.missing_agents_md == []


def test_write_dag_readiness_report(tmp_path: Path) -> None:
    report = DagReadinessReport(ok=True, dag_path="a", registry_path="b")
    out = write_dag_readiness_report(report, tmp_path / "dag_readiness.json")
    assert out.is_file()
