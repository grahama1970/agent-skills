[project]
name = "episodic-archiver"
version = "0.1.0"
description = "Archive conversation episodes to the memory graph."
readme = "SKILL.md"
requires-python = ">=3.10.11,<3.13"
dependencies = [
    "graph-memory @ git+https://github.com/grahama1970/graph-memory-operator.git",
    "httpx>=0.24.0",
    "json-repair>=0.30",
    "loguru>=0.7.0,<0.7.3",
    "python-dotenv>=1.0.0",
    "scillm @ git+https://github.com/grahama1970/scillm.git",
    "tqdm",
    "typer>=0.9.0",
]

[tool.uv]
# No special config needed
