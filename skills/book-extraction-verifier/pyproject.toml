[project]
name = "book-extraction-verifier-skill"
version = "0.1.0"
description = "Verify full-book extraction artifacts and memory recall readiness."
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.24.0",
  "typer>=0.12",
]

[tool.uv]
package = false
