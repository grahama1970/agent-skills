# Synthetic Tau fixture for Live Evidence sanity

Tau treats every agent output as a claim, not a fact. A run advances only after
typed receipt admission, goal-bound validation, and explicit effect policy.

This fixture is not a claim about the current public repository. It exists only
to prove that the Live Evidence sanity path executes real ripgrep and preserves
a source locator.
