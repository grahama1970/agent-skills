# Generated by test-lab — blind adversarial evaluation
"""Regression tests for DriveWealth meeting-quality oracle generation."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "scripts"))

import eval_drivewealth_meeting_quality as quality  # noqa: E402


def test_authored_answer_key_is_used_for_expected_answer() -> None:
    root = Path(__file__).resolve().parents[1]

    answer = quality._expected_answer(
        "Design an internal agent platform with version negotiation.",
        question_id="DW-AI-02-T01",
        root=root,
        category="architecture_or_topology_question",
        chain=["memory", "create-figure"],
        response_shape="topology_or_system_boundary_explanation",
    )

    assert "Authored reviewed answer:" in answer
    assert "separate the application graph contract" in answer.lower()
    assert "required lane" not in answer.lower()
    assert "create-figure" not in answer
