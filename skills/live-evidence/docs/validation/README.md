# Validation

`package-validation.json` records the bounded packaging proof. It distinguishes
what was actually executed from optional lanes that require Graham's prepared
host, audio devices, models, credentials, or running services.

The authoritative command remains:

```bash
./sanity.sh
```
