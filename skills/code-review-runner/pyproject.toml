[project]
name = "code-review-runner"
version = "1.0.0"
description = "Deterministic code review: T0 validators + LLM findings + fix validation"
requires-python = ">=3.11,<3.13"
dependencies = [
    "httpx>=0.24",
    "loguru>=0.7,<0.7.3",
    "pydantic>=2.0",
    "typer>=0.9",
]
