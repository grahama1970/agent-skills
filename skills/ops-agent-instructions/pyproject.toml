[project]
name = "ops-agent-instructions"
version = "0.1.0"
description = "Audit global agent instruction files for parity, required clauses, and proof-boundary reporting."
requires-python = ">=3.11"
dependencies = [
    "loguru>=0.7.0",
    "typer>=0.12.0",
]

[tool.uv]
package = false
