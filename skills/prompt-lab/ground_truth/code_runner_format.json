{
  "name": "code-runner-format",
  "description": "Eval cases for code-runner LLM format instruction. Tests whether the prompt produces parseable file blocks or diffs across 4 scenarios.",
  "validation_type": "code_format",
  "cases": [
    {
      "id": "cr_new_file",
      "input": "Create a new file /tmp/calculator.py with functions add(a, b) and multiply(a, b) that return the correct results.",
      "context": "Task type: NEW FILE CREATION. No existing file to edit. CWD: /tmp",
      "expected": {
        "format": "file_block",
        "has_file_header": true,
        "file_path_present": true,
        "file_path_contains": "calculator.py",
        "complete_file": true,
        "parseable": true
      },
      "notes": "Simplest case. LLM should return ### FILE: /tmp/calculator.py with complete content."
    },
    {
      "id": "cr_edit_existing",
      "input": "Read /home/graham/workspace/experiments/pi-mono/.pi/skills/classifier-lab/scripts/concurrent_run.py. The function phase_data_validation(data_dir, modality) returns total_samples: 0 when data_dir is a HuggingFace dataset name. Fix it to load the dataset and count classes. The file is 600+ lines — return ONLY the changes, not the entire file.",
      "context": "Task type: EDIT EXISTING FUNCTION in large file (600+ lines). Must NOT overwrite entire file. CWD: /home/graham/workspace/experiments/pi-mono",
      "expected": {
        "format": "diff_or_partial",
        "is_diff": true,
        "has_diff_headers": true,
        "does_not_overwrite_full_file": true,
        "targets_specific_function": true,
        "parseable": true
      },
      "notes": "Critical case. Current prompt FAILS here — LLM returns just the function and overwrites 600-line file with 30 lines. Must return unified diff or clearly marked partial edit."
    },
    {
      "id": "cr_multi_file",
      "input": "Add a new API endpoint POST /api/projects/classifier-lab/rerun/:id to packages/ux-lab/server/index.ts that accepts {backbones, gate_f1, max_rounds} and submits a Switchboard manifest. Also update the React component packages/ux-lab/src/components/sparta/explorer/ClassifierLabView.tsx to add a RerunButton that calls this endpoint.",
      "context": "Task type: MULTI-FILE EDIT. Two files need changes. CWD: /home/graham/workspace/experiments/pi-mono",
      "expected": {
        "format": "multiple_blocks",
        "file_count_gte": 2,
        "each_has_file_header": true,
        "each_has_path": true,
        "parseable": true
      },
      "notes": "Multi-file case. LLM must return separate FILE blocks or diffs for each file."
    },
    {
      "id": "cr_fix_import",
      "input": "The code crashes with: ModuleNotFoundError: No module named 'sentencepiece'. Fix backbone_train_loop.py to handle the missing sentencepiece gracefully by trying the import and falling back to use_fast=False for the tokenizer.",
      "context": "Task type: FIX IMPORT ERROR. Small targeted change in one file. CWD: /home/graham/workspace/experiments/pi-mono/.pi/skills/classifier-lab",
      "expected": {
        "format": "diff_or_file_block",
        "targets_import_section": true,
        "minimal_change": true,
        "parseable": true
      },
      "notes": "Import fix — should be a small diff or targeted edit, not a full file rewrite."
    }
  ]
}
