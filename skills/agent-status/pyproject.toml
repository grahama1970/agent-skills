[project]
name = "agent-status"
version = "0.1.0"
description = "Artifact-backed project-agent status surfaces"
requires-python = ">=3.11"
dependencies = ["typer>=0.12.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
