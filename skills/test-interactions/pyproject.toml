[project]
name = "test-interactions"
version = "0.1.0"
description = "Systematic UI interaction testing with screenshots and burst-mode animation capture"
requires-python = ">=3.11,<3.13"
dependencies = [
    "httpx>=0.27.0",
    "typer>=0.9.0",
    "loguru>=0.7.0,<0.7.3",
    "websocket-client>=1.6.0",
    "pillow>=10.4.0",
]
