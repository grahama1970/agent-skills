[project]
name = "create-context"
version = "1.0.0"
description = "Generate CONTEXT.md for agent handoff"
requires-python = ">=3.11,<3.13"
dependencies = [
    "typer>=0.9.0",
    "rich>=13.0.0",
    "loguru>=0.7.0,<0.7.3",
]

[project.scripts]
create-context = "context:app"
