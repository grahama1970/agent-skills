PROBLEM: Ask budgets a queued Surf lock owner at 900 seconds even though a healthy browser submit may hold the per-socket lock for the full 2,400-second worker timeout, so the waiter expires before the owner is allowed to finish.
CHANGE: skills/ask/src/ask/tau_dag.py: change DEFAULT_BROWSER_SUBMIT_TIMEOUT_SECONDS from 900 to 2400.
PROOF_COMMAND: uv run --project skills/ask python -c 'import ask.tau_dag as d; r=d.infer_compile_input("Roundtable webgpt and webclaude concurrently.", repo="local/agent-skills", target="lock-budget", immutable_goal="Both browser seats complete.", handlers=["webgpt","webclaude"]); lock=d._browser_lock_timeout_seconds(r); assert lock >= d.DEFAULT_BROWSER_WORKER_TIMEOUT_SECONDS, (lock, d.DEFAULT_BROWSER_WORKER_TIMEOUT_SECONDS)'
WHY_THIS_UNBLOCKS: Surf intentionally serializes non-streaming commands per socket, so a queued Ask lane must permit one complete browser-worker duration for the owner ahead of it. Raising this shared slot budget to 2,400 seconds also expands Ask’s outer browser-command envelope, allowing the owner to release the lock naturally without unsafe --no-lock interleaving.
