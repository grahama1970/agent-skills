# Generated by test-lab — blind adversarial evaluation
"""test_setup_e2e_sanity - tests.

Purpose: Auto-generated module docstring. Review for accuracy.
Inputs/Outputs/Failures: See functions below.
"""

from __future__ import annotations

from ask.setup_bootstrap import run_setup_e2e_sanity


def test_setup_e2e_sanity_reports_required_dry_run_checks() -> None:
    result = run_setup_e2e_sanity(live=False)

    assert result["schema"] == "ask.setup_e2e_sanity.v1"
    assert result["status"] == "pass"
    assert result["failed_required"] == []
    check_names = {check["name"] for check in result["checks"]}
    assert check_names == {
        "memory_upsert_recall",
        "dense_recall",
        "scillm_health_json",
        "ask_config_doctor",
    }
