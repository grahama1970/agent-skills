# Generated by test-lab — blind adversarial evaluation
"""test_ask_routing_webgpt_page - tests.

Purpose: Auto-generated module docstring. Review for accuracy.
Inputs/Outputs/Failures: See functions below.
"""

from __future__ import annotations

import pathlib
import sys

THIS_DIR = pathlib.Path(__file__).resolve().parent
SRC_DIR = THIS_DIR.parent / "src"
if str(SRC_DIR) not in sys.path:
    sys.path.insert(0, str(SRC_DIR))

from ask.ask_routing import _parse_webgpt_page_review_query


def test_force_flag():
    matched, page_id, _, _, _, force = _parse_webgpt_page_review_query(
        ["/review-page", "coverage", "--force"]
    )
    assert matched
    assert page_id == "coverage"
    assert force is True
