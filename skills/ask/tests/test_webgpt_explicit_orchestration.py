# Generated by test-lab — blind adversarial evaluation
"""Explicit $ask webgpt aliases are deprecated and must fail closed."""

from __future__ import annotations

import pathlib
import sys

import pytest

SRC = pathlib.Path(__file__).resolve().parents[1] / "src"
if str(SRC) not in sys.path:
    sys.path.insert(0, str(SRC))

from ask.model_aliases import resolve_model_alias_route


def test_webgpt_alias_is_rejected():
    with pytest.raises(ValueError, match="removed from /ask"):
        resolve_model_alias_route(
            ["webgpt", "product design only: collab skill?"],
            scillm_base_url="http://localhost:4001",
            scillm_api_key="sk-dev-proxy-123",
        )
