{
  "acceptance_contract": [
    "Repair policy is externalized in config/ask_dag_repair_policy.yaml with skill-published hints under skills/*/config/ask_dag_repair_hints.yaml.",
    "Timeout profiles live in config/ask_dag_timeout_profiles.yaml and apply when nodes omit input.timeout.",
    "Dogpile stage2_github no longer crashes when primaryLanguage or readme objects are null.",
    "Deterministic tests cover timeout bump repair, partial-artifact recovery, and github metadata null handling.",
    "Live negative DAG sanity still fails closed with dag_layer_failed."
  ],
  "accepted_at": "20260527T184103Z",
  "blockers": [],
  "notes": "Builds on accepted phase-24 natural-language DAG orchestration.",
  "phase_id": "phase-25-ask-dag-auto-repair",
  "schema": "plan_iterate.phase_status.v1",
  "status": "accepted",
  "title": "Ask DAG auto-repair policy and dogpile stage2 hardening",
  "validation_commands": [
    {
      "command": "PYTHONPATH=src pytest -q tests/test_ask_dag.py tests/test_dag_repair.py",
      "exit_code": 0,
      "log": "validation-logs/test-ask-dag-repair.log"
    },
    {
      "command": "./scripts/dag_negative_sanity.py --output-root /tmp/ask-dag-negative-proof2 --ask-id ask-dag-negative-proof2",
      "exit_code": 0,
      "log": "validation-logs/dag-negative-sanity.json"
    }
  ]
}
