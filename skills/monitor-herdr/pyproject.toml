[project]
name = "agent-skills-monitor-herdr"
version = "0.1.0"
requires-python = ">=3.11,<3.13"
dependencies = [
  "loguru>=0.7.0,<0.7.3",
  "pytest>=8.0.0",
  "python-dotenv>=1.0.0",
  "typer>=0.12.0",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
