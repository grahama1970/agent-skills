# Code Runner Review: 1

**Rounds:** 2
**Best score:** 1.000 (round 2)
**DoD passed:** True

## Round Trajectory
| Round | Score | Strategy | Status | Errors |
|-------|-------|----------|--------|--------|
| 1 | 0.000 | direct_fix | discard | 0 (unknown) |
| 2 | 1.000 | structured_analysis | keep | 0 (unknown) |

diff --git a/packages/ux-lab/server/index.ts b/packages/ux-lab/server/index.ts
index 4e885f4f..91f50dab 100644
--- a/packages/ux-lab/server/index.ts
+++ b/packages/ux-lab/server/index.ts
@@ -1,5 +1,5 @@
 /**
- * UX Lab API Server
+* UX Lab API Server
  *
  * Thin proxy layer — all data flows through the memory daemon Unix socket.
  * No bespoke reimplementations of skill logic. No hardcoded file paths.
@@ -93,7 +93,7 @@ function asRows(response: any): AnyObj[] {
 
 app.get('/api/posture/frameworks', async (_req, res) => {
   try {
-    const [controlsRaw, qraRaw] = await Promise.all([
+    const [controlsRaw, qraRaw, relRaw] = await Promise.all([
       postMemory('/list', {
         collection: 'sparta_controls',
         return_fields: ['control_id', 'source_framework', 'nrs_score', 'weaknesses'],
@@ -104,27 +104,42 @@ app.get('/api/posture/frameworks', async (_req, res) => {
         return_fields: ['control_id'],
         group_by: 'control_id',
         limit: 0
+      }),
+      postMemory('/list', {
+        collection: 'sparta_relationships',
+        return_fields: ['control_id', 'from_control_id', 'to_control_id', 'source_control_id', 'target_control_id'],
+        limit: 0
       })
     ])
 
     const controls = asRows(controlsRaw)
     const qraRows = asRows(qraRaw)
     const qraSet = new Set(qraRows.map((r) => String(r.control_id || '')).filter(Boolean))
+    const relRows = asRows(relRaw)
+    const relSet = new Set<string>()
+    for (const r of relRows) {
+      for (const key of ['control_id', 'from_control_id', 'to_control_id', 'source_control_id', 'target_control_id']) {
+        const v = r[key]
+        if (v) relSet.add(String(v))
+      }
+    }
 
-    const byFramework = new Map<string, { name: string; total: number; withQRAs: number }>()
+    const byFramework = new Map<string, { name: string; total: number; withQRAs: number; withRels: number }>()
     for (const c of controls) {
       const name = String(c.source_framework || 'Unknown')
       const id = String(c.control_id || '')
-      if (!byFramework.has(name)) byFramework.set(name, { name, total: 0, withQRAs: 0 })
+      if (!byFramework.has(name)) byFramework.set(name, { name, total: 0, withQRAs: 0, withRels: 0 })
       const bucket = byFramework.get(name)!
       bucket.total += 1
       if (id && qraSet.has(id)) bucket.withQRAs += 1
+      if (id && relSet.has(id)) bucket.withRels += 1
     }
 
     const frameworks = [...byFramework.values()].map((f) => ({
       name: f.name,
       total: f.total,
       withQRAs: f.withQRAs,
+      withRels: f.withRels,
       pct: f.total > 0 ? Number((f.withQRAs / f.total).toFixed(4)) : 0
     }))
 
@@ -144,28 +159,48 @@ app.get('/api/posture/frameworks', async (_req, res) => {
 app.get('/api/posture/families/:framework', async (req, res) => {
   try {
     const framework = String(req.params.framework || '')
-    const controlsRaw = await postMemory('/list', {
-      collection: 'sparta_controls',
-      where: { source_framework: framework },
-      return_fields: ['control_id', 'nrs_score'],
-      limit: 0
-    })
+    const [controlsRaw, qraRaw, relRaw] = await Promise.all([
+      postMemory('/list', {
+        collection: 'sparta_controls',
+        where: { source_framework: framework },
+        return_fields: ['control_id', 'nrs_score'],
+        limit: 0
+      }),
+      postMemory('/list', {
+        collection: 'sparta_qra',
+        return_fields: ['control_id'],
+        group_by: 'control_id',
+        limit: 0
+      }),
+      postMemory('/list', {
+        collection: 'sparta_relationships',
+        return_fields: ['control_id', 'from_control_id', 'to_control_id', 'source_control_id', 'target_control_id'],
+        limit: 0
+      })
+    ])
     const controls = asRows(controlsRaw)
+    const qraSet = new Set(asRows(qraRaw).map((r) => String(r.control_id || '')).filter(Boolean))
+    const relSet = new Set<string>()
+    for (const r of asRows(relRaw)) {
+      for (const key of ['control_id', 'from_control_id', 'to_control_id', 'source_control_id', 'target_control_id']) {
+        const v = r[key]
+        if (v) relSet.add(String(v))
+      }
+    }
 
-    const families = new Map<string, { family: string; total: number; pass: number; partial: number; fail: number }>()
+    const families = new Map<string, { family: string; total: number; withQRAs: number; withRels: number; pct: number }>()
     for (const c of controls) {
       const cid = String(c.control_id || '')
       const fam = (cid.match(/^([A-Z]{2})[-_]/)?.[1] ?? cid.slice(0, 2).toUpperCase() || 'UN')
-      if (!families.has(fam)) families.set(fam, { family: fam, total: 0, pass: 0, partial: 0, fail: 0 })
+      if (!families.has(fam)) families.set(fam, { family: fam, total: 0, withQRAs: 0, withRels: 0, pct: 0 })
       const b = families.get(fam)!
       b.total += 1
-      const nrs = Number(c.nrs_score ?? 0)
-      if (nrs >= 0.8) b.pass += 1
-      else if (nrs >= 0.6) b.partial += 1
-      else b.fail += 1
+      if (cid && qraSet.has(cid)) b.withQRAs += 1
+      if (cid && relSet.has(cid)) b.withRels += 1
     }
+    const out = [...families.values()].map((f) => ({ ...f, pct: f.total ? Number((f.withQRAs / f.total).toFixed(4)) : 0 }))
 
-    res.json({ framework, families: [...families.values()].sort((a, b) => a.family.localeCompare(b.family)) })
+    res.json({ framework, families: out.sort((a, b) => a.family.localeCompare(b.family)) })
   } catch (err: any) {
     if (isMemoryUnavailableError(err)) return res.status(502).json({ error: 'Memory daemon unavailable' })
     res.status(500).json({ error: 'Posture families failed', detail: String(err) })
@@ -216,19 +251,22 @@ app.get('/api/posture/gaps', async (_req, res) => {
       if (nrs < 0.6 || weaknesses > 2 || !hasQra || !hasRel) manualReview += 1
 
       if ((!hasQra || !hasRel || !hasPolicy || evidenceExpired || nrs < 0.6) && details.length < 20) {
+        const reasons = []
+        if (!hasQra) reasons.push('missing_qra')
+        if (!hasRel) reasons.push('no_relationships')
+        if (!hasPolicy) reasons.push('unmapped_policy')
+        if (evidenceExpired) reasons.push('expired_evidence')
+        if (nrs < 0.6) reasons.push('low_nrs')
         details.push({
           control_id: cid,
-          framework: c.source_framework ?? null,
-          nrs_score: nrs,
-          weaknesses,
-          missingQRA: !hasQra,
-          hasRelationship: hasRel,
-          mappedPolicy: hasPolicy,
-          evidenceExpired
+          name: c.name ?? c.control_name ?? c.title ?? undefined,
+          source_framework: c.source_framework ?? undefined,
+          reason: reasons.join(','),
+          qraCount: hasQra ? 1 : 0,
+          relCount: hasRel ? 1 : 0
         })
       }
     }
-
     res.json({ missingQRAs, noRelationships, unmappedPolicies, expiredEvidence, manualReview, details })
   } catch (err: any) {
     if (isMemoryUnavailableError(err)) return res.status(502).json({ error: 'Memory daemon unavailable' })
@@ -250,7 +288,7 @@ app.get('/api/posture/risks', async (req, res) => {
       .map((c) => ({
         control_id: c.control_id,
         name: c.name ?? c.control_name ?? c.title ?? String(c.control_id ?? ''),
-        framework: c.source_framework ?? null,
+        source_framework: c.source_framework ?? null,
         weaknesses: Number(c.weaknesses ?? 0),
         nrs_score: Number(c.nrs_score ?? 0)
       }))