Everything works. Here's a summary of the changes:

**New file: `.pi/skills/ingest-code/treesitter_scan.py`** (99 lines)
- `treesitter_scan_dir(directory, scope)` — runs `treesitter/run.sh scan <dir>`, parses JSON output, stores each symbol to memory via Unix socket at `/run/user/1000/embry/memory.sock`
- Tags each symbol as `["codebase", "symbol", "<kind>", "<name>", "<file_stem>"]` where kind is function/class/method
- Stores signature + docstring (truncated to 800 chars) as the solution
- `_learn_symbol()` — httpx call via UDS transport (same pattern as `_learn_http` in main file)

**Modified: `.pi/skills/ingest-code/ingest_code.py`** (+11 net lines)
- Added `from treesitter_scan import treesitter_scan_dir`
- Added `--treesitter` flag (default False) to `rescan` command
- After each codebase's file-level scan loop, calls `treesitter_scan_dir` if flag is set
- Added `treesitter_symbols` count to the JSON summary output
