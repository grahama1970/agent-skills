The `sci_file` is the skills-ci report. `generate_fix_plan.py` reads from `--report`. Now I have the full picture.

Here's the replacement:

```diff
--- a/.pi/skills/monitor-codebase/run.sh
+++ b/.pi/skills/monitor-codebase/run.sh
@@ -345,60 +345,42 @@
 
   echo "Report: $report_file ($total_issues issues: $summary_text)"
 
-  # Step 10: Fix dispatch via /agent-inbox → /subagent-service (Docker)
+  # Step 10: Deterministic fix via skills-ci generate_fix_plan → /orchestrate
   if [[ "$do_fix" == "true" && "$total_issues" -gt 0 ]]; then
-    echo "[10/10] Dispatching fix via /agent-inbox → /subagent-service ($total_issues issues)..."
-    local task_file="$ARTIFACTS_DIR/${project_name}_0N_FIX_TASKS.md"
-
-    # Build violation summary for the task file
-    local violation_summary
-    violation_summary=$(python3 -c "
-import json
-qc = json.load(open('$qc_file'))
-bp = json.loads('$bp_violations') if '$bp_violations' != '[]' else []
-lines = []
-for v in bp:
-    lines.append(json.dumps(v))
-for v in qc.get('violations', [])[:20]:
-    lines.append(json.dumps(v))
-print('\n'.join(lines))
-" 2>/dev/null || echo "$bp_violations")
-
-    cat > "$task_file" <<TASKEOF
-# Fix Codebase Violations: $project_name
-
-## Context
-monitor-codebase scan found $total_issues issues ($summary_text).
-
-## Violations
-\`\`\`json
-$violation_summary
-\`\`\`
-
-## Instructions
-1. Fix each violation following the rules in the relevant \`best-practices-*\` SKILL.md.
-2. For banned imports: \`logging\` → \`loguru\`, \`requests\` → \`httpx\`, \`argparse\` → \`typer\`.
-3. For inline prompts: move to /prompt-lab for versioning and evaluation.
-4. For handwritten tests: replace with /test-lab blind tests.
-5. For regex classifiers: use /taxonomy or /extract-entities.
-6. Code must comply with best-practices-python/react/kde/skills/streamdeck as applicable.
-
-## Blind Evaluation
-This task uses the Blind Evaluation Gate. The coding agent:
-- MUST NOT read test files directly
-- Will receive only: rule category, natural language failure description, attempt count
-- Has up to 5 retries before escalation
-TASKEOF
-
-    # Research fix strategy
-    run_skill dogpile search "fix common code violations: $summary_text" --no-interactive 2>/dev/null || true
-
-    # Dispatch via agent-inbox
-    run_skill agent-inbox send \
-      --to "$project_name" \
-      --type bug \
-      --priority normal \
-      --context-file "$task_file" \
-      "Fix $total_issues violations found by monitor-codebase ($summary_text)" 2>/dev/null || true
-
-    echo "Fix task dispatched to $project_name"
+    echo "[10/10] Fixing $total_issues issues via skills-ci fix plan + /orchestrate..."
+
+    # 10a: Safety commit — revert point if fixes go wrong
+    if (cd "$project_path" && git diff --quiet && git diff --cached --quiet) 2>/dev/null; then
+      echo "  Working tree clean, no safety commit needed."
+    else
+      echo "  Creating safety commit (revert point)..."
+      (cd "$project_path" && git add -A && git commit -m "safety: pre-fix checkpoint from monitor-codebase [skip ci]") 2>/dev/null || true
+    fi
+
+    # 10b: Generate YAML fix plan from skills-ci report
+    local fix_plan="$ARTIFACTS_DIR/${project_name}_fix_plan_${TIMESTAMP}.yaml"
+    if [[ -f "$sci_file" ]] && python3 -c "import json; d=json.load(open('$sci_file')); exit(0 if 'violations' in d else 1)" 2>/dev/null; then
+      echo "  Generating fix plan from skills-ci report..."
+      (cd "$SKILLS_DIR/skills-ci" && uv run python generate_fix_plan.py \
+        --report "$sci_file" \
+        --output "$fix_plan" 2>&1) || true
+    else
+      echo "  No skills-ci violations report available, skipping fix plan generation."
+    fi
+
+    # 10c: Execute fix plan via /orchestrate (deterministic, no subagents)
+    if [[ -f "$fix_plan" ]]; then
+      echo "  Executing fix plan via /orchestrate..."
+      (cd "$project_path" && run_skill_timed orchestrate run "$fix_plan" 2>&1) || {
+        echo "  WARNING: /orchestrate failed or timed out. Manual review needed: $fix_plan"
+      }
+    else
+      echo "  No fix plan generated (no Tier 2 violations or generation failed)."
+    fi
+
+    # 10d: Commit any fixes applied
+    if (cd "$project_path" && ! git diff --quiet) 2>/dev/null || (cd "$project_path" && ! git diff --cached --quiet) 2>/dev/null; then
+      echo "  Committing fixes..."
+      (cd "$project_path" && git add -A && git commit -m "fix: auto-fix $total_issues violations from monitor-codebase ($summary_text)
+
+Generated by: monitor-codebase --fix → skills-ci generate_fix_plan → /orchestrate
+Report: $report_file") 2>/dev/null || true
+      echo "  Fixes committed."
+    else
+      echo "  No changes to commit after fix pass."
+    fi
   fi
 }
```

This replaces the agent-inbox/subagent-service dispatch (0% success) with a deterministic pipeline:
1. **Safety commit** — creates a revert point if there are uncommitted changes
2. **generate_fix_plan.py** — reads the `sci_file` (already produced by Step 7) and outputs a YAML plan with only Tier 2 violations
3. **orchestrate run** — executes the YAML plan deterministically via code-runner
4. **Commit fixes** — commits any changes made, with a descriptive message referencing the report

The `--fix` gate is preserved — scan-only mode (`do_fix == false`) skips this entire block.
