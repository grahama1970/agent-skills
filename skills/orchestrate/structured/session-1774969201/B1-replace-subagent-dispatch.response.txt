Now I have the full picture. Replace lines 348-405 with the simplified approach.

```diff
--- a/.pi/skills/monitor-codebase/run.sh
+++ b/.pi/skills/monitor-codebase/run.sh
@@ -345,61 +345,30 @@ import json
 
   echo "Report: $report_file ($total_issues issues: $summary_text)"
 
-  # Step 10: Fix dispatch via /agent-inbox → /subagent-service (Docker)
+  # Step 10: Fix violations via skills-ci fix plan → orchestrate
   if [[ "$do_fix" == "true" && "$total_issues" -gt 0 ]]; then
-    echo "[10/10] Dispatching fix via /agent-inbox → /subagent-service ($total_issues issues)..."
-    local task_file="$ARTIFACTS_DIR/${project_name}_0N_FIX_TASKS.md"
-
-    # Build violation summary for the task file
-    local violation_summary
-    violation_summary=$(python3 -c "
-import json
-qc = json.load(open('$qc_file'))
-bp = json.loads('$bp_violations') if '$bp_violations' != '[]' else []
-lines = []
-for v in bp:
-    lines.append(json.dumps(v))
-for v in qc.get('violations', [])[:20]:
-    lines.append(json.dumps(v))
-print('\n'.join(lines))
-" 2>/dev/null || echo "$bp_violations")
-
-    cat > "$task_file" <<TASKEOF
-# Fix Codebase Violations: $project_name
-
-## Context
-monitor-codebase scan found $total_issues issues ($summary_text).
-
-## Violations
-\`\`\`json
-$violation_summary
-\`\`\`
-
-## Instructions
-1. Fix each violation following the rules in the relevant \`best-practices-*\` SKILL.md.
-2. For banned imports: \`logging\` → \`loguru\`, \`requests\` → \`httpx\`, \`argparse\` → \`typer\`.
-3. For inline prompts: move to /prompt-lab for versioning and evaluation.
-4. For handwritten tests: replace with /test-lab blind tests.
-5. For regex classifiers: use /taxonomy or /extract-entities.
-6. Code must comply with best-practices-python/react/kde/skills/streamdeck as applicable.
-
-## Blind Evaluation
-This task uses the Blind Evaluation Gate. The coding agent:
-- MUST NOT read test files directly
-- Will receive only: rule category, natural language failure description, attempt count
-- Has up to 5 retries before escalation
-TASKEOF
-
-    # Research fix strategy
-    run_skill dogpile search "fix common code violations: $summary_text" --no-interactive 2>/dev/null || true
-
-    # Dispatch via agent-inbox
-    run_skill agent-inbox send \
-      --to "$project_name" \
-      --type bug \
-      --priority normal \
-      --context-file "$task_file" \
-      "Fix $total_issues violations found by monitor-codebase ($summary_text)" 2>/dev/null || true
-
-    echo "Fix task dispatched to $project_name"
+    echo "[10/10] Fixing $total_issues violations via skills-ci → orchestrate..."
+    local project_path
+    project_path=$(resolve_project_path "$project_name")
+    local fix_plan="$ARTIFACTS_DIR/${project_name}_fix_plan.yaml"
+
+    # Safety commit as revert point
+    git -C "$project_path" add -A && \
+      git -C "$project_path" commit -m "chore: pre-fix safety checkpoint (monitor-codebase)" --allow-empty 2>/dev/null || true
+
+    # Generate YAML fix plan from violations
+    if python3 "$SKILLS_DIR/skills-ci/generate_fix_plan.py" \
+         --scan-report "$report_file" \
+         --output "$fix_plan" 2>&1; then
+
+      # Execute fix plan via orchestrate
+      "$SKILLS_DIR/orchestrate/run.sh" "$fix_plan" 2>&1 || true
+
+      # Commit any fixes
+      git -C "$project_path" add -A && \
+        git -C "$project_path" commit -m "fix: auto-fix violations from monitor-codebase scan ($summary_text)" 2>/dev/null || true
+      echo "Fix cycle complete for $project_name"
+    else
+      echo "WARN: Failed to generate fix plan for $project_name" >&2
+    fi
   fi
 }
```

This replaces the entire Step 10 block with 4 simple operations:

1. **Safety commit** — `git commit --allow-empty` creates a revert point
2. **Generate fix plan** — calls `generate_fix_plan.py` with the scan report, outputs YAML
3. **Execute fixes** — calls `orchestrate/run.sh` with the YAML plan
4. **Commit fixes** — commits whatever orchestrate changed

Key decisions:
- Removed all agent-inbox/subagent-service/dogpile machinery (0% success rate)
- Kept `--fix` gate — the `if [[ "$do_fix" == "true"` guard is preserved
- Scan-only mode skips this entire block (unchanged)
- Failures are non-fatal (`|| true` on git/orchestrate, `WARN` on plan generation failure)

