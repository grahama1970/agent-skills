{"summary":"Fixed formatting bugs and updated logger import","approach":"direct_fix","files_changed":["src/formatter.py"]}
---
### FILE: src/formatter.py
```diff
--- a/src/formatter.py
+++ b/src/formatter.py
@@ -1,17 +1,17 @@
 """String formatting utilities with bugs."""
-import logging  # BUG: should use loguru
+from loguru import logger
 
 
 def format_name(first, last):
     """Format a full name."""
-    return f"{last} {first}"  # BUG: wrong order
+    return f"{first} {last}"
 
 
 def format_currency(amount, symbol="$"):
     """Format a currency amount."""
-    return f"{symbol}{amount}"  # BUG: no decimal formatting
+    return f"{symbol}{amount:,.2f}"
 
 
 def format_list(items):
     """Format a list as comma-separated string."""
-    return str(items)  # BUG: should join, not str()
+    return ", ".join(items) if items else ""
```
```python
"""String formatting utilities with bugs."""
from loguru import logger


def format_name(first, last):
    """Format a full name."""
    return f"{first} {last}"


def format_currency(amount, symbol="$"):
    """Format a currency amount."""
    return f"{symbol}{amount:,.2f}"


def format_list(items):
    """Format a list as comma-separated string."""
    return ", ".join(items) if items else ""
```