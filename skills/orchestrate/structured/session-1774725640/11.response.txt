Implemented in [ClassifierLabView.tsx](/home/node/workspace/packages/ux-lab/src/components/sparta/explorer/ClassifierLabView.tsx#L1114):
- Normalized trial rows in `TuneTab` so rendering works with real `tune-results.json` shapes:
- Handles `trial`/`round`, `valF1`/`val_f1`, `testF1`/`test_f1`/`f1`, direct HP fields and `hps.*`, plus `passed`/`gate_passed`.
- Fixed round progress circles to always show actual round numbers (`trial`) instead of checkmarks.
- Fixed HP delta analysis to compare normalized, sorted rounds and added F1 delta output between consecutive rounds.
Validation:
- `cd /home/graham/workspace/experiments/pi-mono/packages/ux-lab && npx tsc --noEmit 2>&1 | head -5`
- Failed in this environment because `/home/graham/...` does not exist.
- Equivalent local path check:
- `cd /home/node/workspace/packages/ux-lab && npx tsc --noEmit 2>&1 | head -5`
- No output (no type errors).
- `npm run check` (repo root) passed.
- `./.pi/skills/test-lab/run.sh verify-task 11 /home/node/workspace/.pi/skills/classifier-lab --domain python`
- Fails with pre-existing style checks (`Missing module docstring`, 126/135 passes), not TypeScript errors.