Implemented in [concurrent_run.py](/home/node/workspace/.pi/skills/classifier-lab/scripts/concurrent_run.py):
- `_write_ux_project()` now writes all requested project artifacts under `create-classifier/projects/{id}/`:
- `research.md`
- `research-gate.json` with `{passed, hash, lineCount, memoryVerified}`
- `data.json` with `{path, modality, totalTrain, classCount, classes, gatePassed, gateThreshold, minPerClass}`
- `dataset.parquet` (existing polars path retained)
- `tune-results.json` aggregated across all backbones with `{trials, strategy:'switchboard-concurrent', winningRound}`
- `benchmark.json` with per-backbone `wilson_score_lower`, `gate_passed`, plus `selected_backbone` and `selected_metrics`
- `eval-results.json` copied from winner `eval_details.json` (fallback payload if missing)
- `promote-results.json` with `{ready, model, f1, hf_repo, exported}`
- `phase_data_validation()` now also records `class_counts` for JSONL datasets so `data.json.classes` is populated correctly.
- `run()` now captures `research_md` and passes it into `_write_ux_project()`.
- Added robust winner log-dir derivation and JSON loaders to read winner `eval_details.json` and per-backbone tune outputs.
Validation run:
- Required assertion command output:
- `OK: _write_ux_project exists`
- `python -m py_compile .pi/skills/classifier-lab/scripts/concurrent_run.py` passed.
- `npm run check` passed (repo has 2 existing info-level biome hints unrelated to this file).
- `test-lab/run.sh verify-task ...` could not be executed here because that script path does not exist in this environment (`No such file or directory`).