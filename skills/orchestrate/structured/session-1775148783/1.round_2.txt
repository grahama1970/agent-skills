{"summary":"Fixed divide() to handle zero division correctly and match test expectations","approach":"direct_fix","files_changed":["src/calculator.py"]}
---
### FILE: src/calculator.py
```diff
--- a/src/calculator.py
+++ b/src/calculator.py
@@ -18,7 +18,7 @@ def multiply(a, b):
 def divide(a, b):
     """Divide a by b."""
     if b == 0:
-        raise ValueError("Cannot divide by zero")
+        raise ZeroDivisionError("Cannot divide by zero")
     return a / b
```
```python
"""Simple calculator module with several bugs for burn-in testing."""


def add(a, b):
    """Add two numbers."""
    return a + b


def subtract(a, b):
    """Subtract b from a."""
    return a - b


def multiply(a, b):
    """Multiply two numbers."""
    return a * b  # CORRECT


def divide(a, b):
    """Divide a by b."""
    if b == 0:
        raise ZeroDivisionError("Cannot divide by zero")
    return a / b
```