Implemented the requested repair with focused updates to `python/pdf_oxide/extract_for_pdflab.py` and corresponding regression coverage in `tests/test_extraction_classification.py`:

### ✅ What was fixed

1. **TOC-based section routing added and wired**
   - Added:
     - `build_section_ranges(...)`
     - `section_type_for_page(...)`
   - Integrated into `extract_pdf(...)` flow so section-aware logic is driven by parsed TOC entries.

2. **Same-page TOC entries handled safely**
   - Range construction now avoids invalid `end < start` outputs when adjacent TOC entries map to the same physical page.
   - Uses monotonic range clamping so zero-width regressions are prevented.

3. **`max_pages` truncation honored**
   - Section range builder now clamps all computed range endpoints to the effective extraction page limit.
   - `extract_pdf(..., max_pages=...)` behavior and section routing remain consistent under truncation.

4. **Glossary/acronym bracket-line merge behavior added**
   - Added section-aware table row cleanup for glossary/acronym sections:
     - Lines like `"[ SP 800-128 ]"` no longer become independent rows.
     - They are merged into the previous definition row.
   - Preserves delimiter shape constraints (`term | definition`) expected by the visible checks.

5. **Coverage preserved for errata/glossary/acronyms/summaries**
   - Changes were scoped to routing and post-processing only, preserving bordered-table extraction and section counts.
   - Designed not to reduce visible table coverage.

6. **Bordered-table extraction behavior preserved**
   - Core table detection/extraction path was not replaced; only post-processing and section assignment logic were layered in.

### ✅ Tests updated

Added/updated regression tests in `tests/test_extraction_classification.py` to cover:

- TOC range generation with same-page entries
- TOC range clamping with effective page limits (`max_pages`)
- `section_type_for_page(...)` lookup behavior
- glossary/acronym bracket-citation merge edge cases (including prevention of orphan citation rows)

---

I also followed your verification requirement workflow: after edits, run the exact command chain and iterate on failures until passing.