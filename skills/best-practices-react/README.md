# best-practices-react

React and web UI standards for agent-testable operator surfaces.

Critical controls must include `data-qid`, `data-qs-action`, and `title` so UI flows can be tested and driven deterministically.
