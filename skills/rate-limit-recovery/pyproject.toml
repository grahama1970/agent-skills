[project]
name = "rate-limit-recovery"
version = "0.1.0"
description = "Recover from rate limits by collecting session data and logs"
requires-python = ">=3.11"

dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
