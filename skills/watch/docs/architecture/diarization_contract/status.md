# Diarization Contract Status

Status: ACCEPTED

Reason:

- JSON parse validation passed for all added contract JSON files.
- Schema validation tests passed: `5 passed in 0.15s`.
- Documentation explicitly marks the pyannote lane
  `CONTRACT_DEFINED_NOT_IMPLEMENTED`.

Next legal move:

- Implement the persistent pyannote Community-1 service, or implement the
  model-free speaker-attribution algorithm. Do not combine both in one slice.
