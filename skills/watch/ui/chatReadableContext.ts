export function useChatReadable() {
  return {
    blockGap: 8,
    fontMono: 'var(--font-mono, ui-monospace, SFMono-Regular, Menlo, Consolas, monospace)',
    fontSans: 'var(--font-ui, Inter, system-ui, sans-serif)',
    lineHeight: 1.45,
    metaSize: 12,
    metadataSize: 11,
  }
}
