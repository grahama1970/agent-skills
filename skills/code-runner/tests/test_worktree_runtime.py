# Generated by test-lab — blind adversarial evaluation
"""test_worktree_runtime - tests.

Purpose: Auto-generated module docstring. Review for accuracy.
Inputs/Outputs/Failures: See functions below.
"""

from __future__ import annotations

import importlib.util
import subprocess
import sys
import tempfile
from pathlib import Path


MODULE_PATH = Path(__file__).resolve().parents[1] / "worktree_runtime.py"


def load_module_without_sys_modules_registration():
    spec = importlib.util.spec_from_file_location("worktree_runtime_unregistered", MODULE_PATH)
    assert spec is not None
    assert spec.loader is not None
    module = importlib.util.module_from_spec(spec)
    # Deliberately do not insert into sys.modules.  This reproduces the import
    # style that made @dataclass crash with: AttributeError: 'NoneType' object
    # has no attribute '__dict__'.
    spec.loader.exec_module(module)
    return module


def run(cmd: list[str], cwd: Path) -> subprocess.CompletedProcess[str]:
    proc = subprocess.run(cmd, cwd=str(cwd), text=True, capture_output=True, timeout=30)
    if proc.returncode != 0:
        raise AssertionError(f"{cmd!r} failed\nstdout={proc.stdout}\nstderr={proc.stderr}")
    return proc


def make_repo(parent: Path) -> Path:
    repo = parent / "repo"
    repo.mkdir()
    run(["git", "init"], repo)
    run(["git", "config", "user.email", "test@example.invalid"], repo)
    run(["git", "config", "user.name", "Test User"], repo)
    (repo / "README.md").write_text("hello\n")
    run(["git", "add", "README.md"], repo)
    run(["git", "commit", "-m", "initial"], repo)
    return repo


def test_module_imports_when_not_registered_in_sys_modules() -> None:
    module = load_module_without_sys_modules_registration()
    assert hasattr(module, "DisposableWorktreeRuntime")
    assert hasattr(module, "create_disposable_worktree")


def test_disposable_worktree_context_creates_and_cleans_isolated_checkout() -> None:
    module = load_module_without_sys_modules_registration()
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        repo = make_repo(tmp_path)
        parent = tmp_path / "worktrees"

        with module.disposable_worktree(repo, task_id="task 1", parent_dir=parent) as runtime:
            wt_path = runtime.path
            branch = runtime.branch
            assert wt_path.exists()
            assert (wt_path / "README.md").read_text() == "hello\n"
            assert branch.startswith("code-runner/task-1-")
            (wt_path / "README.md").write_text("changed in worktree\n")
            run(["git", "status", "--porcelain"], wt_path)
            assert (repo / "README.md").read_text() == "hello\n"

        assert not wt_path.exists()
        branches = run(["git", "branch", "--list", branch], repo).stdout.strip()
        assert branches == ""


def test_dirty_overlap_is_detected_in_source_repo() -> None:
    module = load_module_without_sys_modules_registration()
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        repo = make_repo(tmp_path)
        assert module.dirty_paths_for_allowlist(repo, ["README.md"]) == []
        (repo / "README.md").write_text("dirty user change\n")
        assert module.dirty_paths_for_allowlist(repo, ["README.md"]) == ["README.md"]


def test_restore_returns_to_original_checkout_before_best_patch() -> None:
    module = load_module_without_sys_modules_registration()
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        repo = make_repo(tmp_path)
        child_patch = tmp_path / "child.patch"
        with module.disposable_worktree(repo, task_id="rollback", parent_dir=tmp_path / "worktrees") as runtime:
            (runtime.path / "README.md").write_text("from child\n")
            module.export_allowlist_patch(runtime.info, ["README.md"], child_patch)
            (runtime.path / "README.md").write_text("bad regression\n")

            module.restore_patch_base_tree(runtime.info)
            assert (runtime.path / "README.md").read_text() == "hello\n"
            run(["git", "apply", "--whitespace=nowarn", str(child_patch)], runtime.path)
            assert (runtime.path / "README.md").read_text() == "from child\n"


def test_restore_clears_intent_to_add_entries_for_best_patch_reapply() -> None:
    module = load_module_without_sys_modules_registration()
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        repo = make_repo(tmp_path)
        child_patch = tmp_path / "new-files.patch"
        with module.disposable_worktree(repo, task_id="ita-restore", parent_dir=tmp_path / "worktrees") as runtime:
            new_file = runtime.path / "src" / "generated.py"
            new_file.parent.mkdir()
            new_file.write_text("VALUE = 1\n")
            module.export_allowlist_patch(runtime.info, ["src/generated.py"], child_patch)
            assert "src/generated.py" in run(["git", "status", "--short"], runtime.path).stdout

            (runtime.path / "src" / "other.py").write_text("BROKEN = True\n")
            module.restore_patch_base_tree(runtime.info)

            assert run(["git", "status", "--short"], runtime.path).stdout == ""
            run(["git", "apply", "--whitespace=nowarn", str(child_patch)], runtime.path)
            assert new_file.read_text() == "VALUE = 1\n"


def test_changed_paths_drive_patch_export_and_allowlist_gate() -> None:
    module = load_module_without_sys_modules_registration()
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        repo = make_repo(tmp_path)
        patch_file = tmp_path / "actual.patch"
        with module.disposable_worktree(repo, task_id="actual-diff", parent_dir=tmp_path / "worktrees") as runtime:
            (runtime.path / "README.md").write_text("changed without reported write\n")
            (runtime.path / "scratch.txt").write_text("not allowed\n")
            assert module.changed_path_statuses(runtime.info) == [(" M", "README.md"), ("??", "scratch.txt")]
            assert module.changed_paths(runtime.info) == ["README.md", "scratch.txt"]
            ok, violations = module.paths_within_allowlist(module.changed_paths(runtime.info), ["README.md"])
            assert not ok
            assert violations == ["scratch.txt"]

            module.remove_untracked_paths(runtime.info, violations)
            assert module.changed_paths(runtime.info) == ["README.md"]
            ok, violations = module.paths_within_allowlist(module.changed_paths(runtime.info), ["README.md"])
            assert ok
            assert violations == []
            module.export_allowlist_patch(runtime.info, ["README.md"], patch_file)
            patch_text = patch_file.read_text()
            assert "changed without reported write" in patch_text
            assert "scratch.txt" not in patch_text


def main() -> None:
    test_module_imports_when_not_registered_in_sys_modules()
    test_disposable_worktree_context_creates_and_cleans_isolated_checkout()
    test_dirty_overlap_is_detected_in_source_repo()
    test_restore_returns_to_original_checkout_before_best_patch()
    test_restore_clears_intent_to_add_entries_for_best_patch_reapply()
    test_changed_paths_drive_patch_export_and_allowlist_gate()
    print("worktree_runtime tests passed")


if __name__ == "__main__":
    main()
