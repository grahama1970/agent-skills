[project]
name = "vector-store-skill"
version = "0.1.0"
description = "Transient vector store service for fast similarity search"
requires-python = ">=3.10,<3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "numpy",
    "faiss-cpu",
    "pydantic",
    "requests"
]

[tool.hatch.build.targets.wheel]
packages = ["server.py"]
