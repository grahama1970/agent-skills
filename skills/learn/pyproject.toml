[project]
name = "learn"
version = "0.1.0"
description = "Learn from any content type with auto-detection and backend skill routing"
requires-python = ">=3.11"

dependencies = [
    "typer>=0.9.0",
    "rich>=13.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
