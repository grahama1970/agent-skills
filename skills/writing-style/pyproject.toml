[project]
name = "writing-style"
version = "0.1.0"
description = "Canonical cross-provider output contract and its broadcast runtime"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.12",
    "loguru>=0.7",
]

[tool.uv]
package = false
