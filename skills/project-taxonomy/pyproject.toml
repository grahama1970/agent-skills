[project]
name = "project-taxonomy"
version = "0.1.0"
description = "Canonical research-discipline vocabulary and labels for the skills catalog"
requires-python = ">=3.11,<3.13"
dependencies = [
    "typer>=0.9.0",
    "loguru>=0.7.0,<0.7.3",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
