[project]
name = "brave-search"
version = "0.1.0"
description = "Free web and local search via Brave Search API"
requires-python = ">=3.11"

dependencies = [
    "typer>=0.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
