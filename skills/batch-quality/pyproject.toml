[project]
name = "batch-quality"
version = "0.1.0"
description = "Preflight validation and quality gates for batch LLM operations"
requires-python = ">=3.10"
dependencies = [
    "typer>=0.9.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=7.0.0",
]
