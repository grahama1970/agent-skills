[project]
name = "ops-google-calendar"
version = "0.1.0"
description = "Google Calendar read + propose-only scheduling over OAuth"
requires-python = ">=3.11,<3.13"
dependencies = [
    "typer>=0.12",
    "loguru>=0.7",
    "httpx>=0.27",
    "google-auth-oauthlib>=1.2",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
