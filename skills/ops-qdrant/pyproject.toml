[project]
name = "ops-qdrant"
version = "0.1.0"
description = "Read-only Qdrant vector-store health detector (no writes)."
requires-python = ">=3.11"
dependencies = [
    "typer>=0.12.0",
    "httpx>=0.27.0",
    "loguru>=0.7.0",
    "pydantic>=2.6.0",
]

[tool.uv]
package = false
