[project]
name = "pi-skill-resume"
version = "0.1.0"
description = "Deterministic canonical resume validation and claim-bound tailoring"
requires-python = ">=3.11,<3.13"
dependencies = [
    "loguru>=0.7.2,<0.8.0",
    "pyyaml>=6.0,<7.0",
    "typer>=0.16.0,<1.0.0",
]

[tool.uv]
package = false
