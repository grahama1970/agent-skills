[project]
name = "pi-skill-ingest-code"
version = "0.1.0"
description = "Runtime package for the ingest-code skill"
requires-python = ">=3.11,<3.13"
dependencies = [
    "loguru>=0.7.0,<0.7.3",
    "typer>=0.9.0",
    "httpx>=0.27.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
