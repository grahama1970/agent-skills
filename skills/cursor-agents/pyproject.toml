[project]
name = "cursor-agents-skill"
version = "0.1.0"
description = "Cursor Cloud Agents API wrapper for agent-skills"
requires-python = ">=3.11,<3.13"
dependencies = [
  "python-dotenv",
  "requests",
  "typer",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
