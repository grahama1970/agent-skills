[project]
name = "triage-error"
version = "0.1.0"
description = "Classify ambiguous pipeline errors into unambiguous codes"
requires-python = ">=3.11,<3.13"
dependencies = ["typer>=0.12", "loguru>=0.7,<0.7.3"]

[tool.uv]
dev-dependencies = ["pytest>=8"]
