[project]
name = "fixture-table"
version = "0.1.0"
description = "Generate PDF tables with ReportLab for extraction testing"
requires-python = ">=3.10"
dependencies = [
    "reportlab>=4.0.0",
    "typer>=0.9.0",
    "pymupdf>=1.23.0",
]

[project.scripts]
fixture-table = "generate:app"
