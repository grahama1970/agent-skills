[project]
name = "extract-pdf"
version = "0.1.0"
description = "Rust-native PDF extraction skill via pdf_oxide"
requires-python = ">=3.11,<3.13"
dependencies = [
    "typer>=0.12",
    "loguru>=0.7,<0.7.3",
    "httpx>=0.27",
]

[project.scripts]
extract-pdf = "extract_pdf.main:app"
