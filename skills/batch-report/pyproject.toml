[project]
name = "batch-report"
version = "0.1.0"
description = "Post-run analysis and reporting for batch processing jobs"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.9.0",
    "rich>=13.0.0",
]

[tool.uv]
dev-dependencies = []
