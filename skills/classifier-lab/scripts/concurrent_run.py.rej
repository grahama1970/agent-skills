diff a/.pi/skills/classifier-lab/scripts/concurrent_run.py b/.pi/skills/classifier-lab/scripts/concurrent_run.py	(rejected hunks)
@@ -569,7 +569,24 @@ def phase_data_validation(data_dir: str, modality: str) -> dict:
             "error": f"Data path not found or unsupported: {data_dir}",
         }
     else:
-        return {
-            "total_samples": 0,
-            "error": f"Data path not found or unsupported: {data_dir}",
-        }
+        try:
+            from datasets import load_dataset
+            ds = load_dataset(data_dir, split='train[:100]')
+            class_names = ds.features['label'].names
+            n_classes = len(class_names)
+            class_counts = {cls: 0 for cls in class_names}
+            
+            for example in ds:
+                class_counts[class_names[example['label']]] += 1
+            
+            min_per_class = min(class_counts.values())
+            balanced = all(count == min_per_class for count in class_counts.values())
+            
+            return {
+                "n_classes": n_classes,
+                "total_samples": len(ds),
+                "class_counts": class_counts,
+                "min_per_class": min_per_class,
+                "balanced": balanced,
+            }
+        except Exception as e:
