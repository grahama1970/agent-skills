[project]
name = "ops-excalidraw"
version = "0.1.0"
description = "Compile Excalidraw animation-token boards into create-svg scenes"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2",
  "typer>=0.12",
]

[tool.uv]
package = false
