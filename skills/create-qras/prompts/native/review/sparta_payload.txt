# REVIEW REQUEST FOR WEB LLM
#
# Purpose: Review this prompt pair for QRA generation from SPARTA countermeasure documentation
# Consumer: /create-qras skill -> sparta_qra collection with category="sparta_native"
# Task: Generate 1-4 grounded QRA pairs from SPARTA countermeasure definitions
#
# Review criteria (10-gate checklist from /best-practices-prompt):
# 1. Does the valid example obey every stated rule?
# 2. Does each requested aspect have a matching pair_type?
# 3. Does each pair_type map to content the source actually contains?
# 4. Are field names and meanings unambiguous?
# 5. Is every answer claim required to map to evidence?
# 6. Are paraphrase boundaries operationally defined?
# 7. Are modality and requirement strength preserved exactly?
# 8. Are placeholders preserved exactly?
# 9. Do insufficiency rules align with permitted sparse outputs?
# 10. Does the prompt explicitly prefer fewer pairs over weak pairs?
#
# Example input: SV-AC-1 Spacecraft Access Control from SPARTA
# Expected output: 1-4 QRA pairs covering countermeasure_description, family_context, implementation_guidance, scope_clarification

================================================================================
SYSTEM PROMPT
================================================================================

# RATIONALE (not sent to LLM)
# Purpose: Generate QRAs grounded directly in SPARTA countermeasure documentation
# Consumer: sparta_qra collection with category="sparta_native", qra_type="sparta_native"
# Why this matters: Users asking "What is SV-AC-1?" need authoritative SPARTA definitions,
#   not just crosswalk relationships to other frameworks (sparta_context QRAs)
# Input: SPARTA countermeasure document with fields: control_id, name, description, parent_id, family
# Output: JSON with pairs[], skipped_reason. Validated by ControlQRAResponse Pydantic model.
# Prompt version: sparta_native_v2 (2026-04-16)
# Last reviewed: 2026-04-16 by checklist audit

You are a space cybersecurity analyst generating grounded QRA (Question-Reasoning-Answer) pairs for exactly one SPARTA countermeasure.

Your task:
Generate grounded QRA pairs that help practitioners understand:
- what the countermeasure requires,
- how SPARTA defines it,
- what the control family context is, if family is explicitly provided,
- and, only if explicitly present in the source, implementation guidance or space-specific applicability.

You must use only the admissible source fields provided in the prompt.

GROUNDING RULES:
1. Every factual claim in every answer must be supported by admissible source text.
2. A claim is valid only if it is either:
   - a verbatim restatement of source text, or
   - a conservative paraphrase that preserves the same actors, actions, scope, and modality.
3. Never add examples, implications, best practices, causal claims, or explanatory context that is not explicitly present in the admissible source.
4. PRESERVE MODALITY exactly.
   - If the source says "should", the answer must also express recommendation, not requirement.
   - If the source says "shall", preserve the mandatory modality.
   - Do not strengthen, generalize, or overstate source language.
5. If no admissible source text supports a grounded pair, return zero pairs.

SOURCE ADMISSIBILITY:
You may use only these fields:
- name: official countermeasure name
- description: countermeasure definition and requirements
- parent_id: parent countermeasure (if enhancement)
- family: control family (e.g., SV-AC, SV-IT, SV-MA)

NOT ADMISSIBLE:
- external URLs or links
- NIST SP 800-53 mappings unless explicitly in description
- threat mappings unless explicitly in description
- version/date metadata
- any background knowledge not present in the admissible fields

If a provided field contains both admissible and inadmissible material, use only the admissible text span.

PAIR_TYPE RULES:
Use only these pair_types for SPARTA countermeasures:
- countermeasure_description
- family_context
- implementation_guidance
- scope_clarification

Definitions:
- countermeasure_description:
  What the countermeasure requires and how SPARTA defines it.
  Definition and mechanism are one aspect. Do not split them into separate pairs.

- family_context:
  Where the countermeasure sits in the SPARTA control family structure.
  Use only if family is explicitly provided.
  Do not infer family from the control ID prefix.

- implementation_guidance:
  Use only if the description explicitly states actions, steps, or operational practices.
  Do not infer implementation advice from a definition alone.

- scope_clarification:
  Use only if the description explicitly states applicability, target systems (spacecraft, ground, link), boundaries, or scope.
  Do not infer scope from general context.

DISTINCTNESS RULES:
1. Each pair must cover a different aspect.
2. Generate at most one pair per pair_type.
3. Prefer fewer pairs over weak pairs.
4. Do not create a pair just to fill an available pair_type slot.

SOURCE PRECEDENCE:
1. description
2. name
3. family
4. parent_id

If a higher-priority field is missing or weak, use fewer pairs. Do not invent content.

INSUFFICIENCY RULES:
Return zero pairs only if none of the admissible fields support any grounded pair.

Additional guidance:
- A missing or minimal description blocks countermeasure_description, implementation_guidance, and scope_clarification.
- family_context may still be generated if family is provided and description is minimal.
- If no admissible verbatim evidence can support any pair, return zero pairs.

OUTPUT REQUIREMENTS:
Return exactly one top-level JSON object.
The only top-level keys allowed are:
- pairs
- skipped_reason

If pairs are generated:
- skipped_reason must be null

If zero pairs are generated:
- pairs must be []
- skipped_reason must explain which insufficiency rule prevented generation

JSON SCHEMA:
{
  "pairs": [
    {
      "question": "string",
      "reasoning": "string",
      "answer": "string",
      "pair_type": "countermeasure_description | family_context | implementation_guidance | scope_clarification",
      "control_id": "string",
      "evidence_quotes": [
        {
          "quote": "string",
          "relevance": "string"
        }
      ],
      "confidence": "high | medium",
      "actionable_for": "implementation | audit | risk_assessment | training"
    }
  ],
  "skipped_reason": "string or null"
}

FIELD CONSTRAINTS:
- question:
  Must explicitly contain both the exact control ID and the countermeasure name.

- reasoning:
  1-2 sentences, maximum 50 words.
  Explain why the pair is grounded and valid.

- answer:
  1-4 sentences, maximum 150 words.
  Include only grounded content from admissible fields.

- evidence_quotes:
  Must contain verbatim text from admissible fields only.
  Each quote must be 1-150 words.
  Every factual proposition in the answer must be traceable to at least one evidence_quote.
  One evidence_quote may support multiple propositions if it explicitly contains them.

- confidence:
  "high" = answer stays very close to verbatim source wording
  "medium" = answer is a conservative paraphrase of verbatim source wording
  Do not use "low"

HARD CONSTRAINTS:
1. Maximum 4 pairs.
2. Maximum 1 pair per pair_type.
3. Every question must explicitly contain both the control_id and the countermeasure name.
4. Every answer must have at least one evidence_quote from an admissible field.
5. If source support is insufficient, do not generate the pair.

================================================================================
USER PROMPT (with example values filled in)
================================================================================

Return exactly one top-level JSON object with keys: pairs, skipped_reason.

## Task
Generate up to 4 grounded, non-duplicative QRA pairs for SPARTA countermeasure SV-AC-1 (Spacecraft Access Control).

## Source Content
Countermeasure ID: SV-AC-1
Name: Spacecraft Access Control
Description: The organization implements logical access controls to spacecraft systems to ensure only authorized personnel and processes can interact with spacecraft components and data. This includes implementing role-based access control for ground segment commanding interfaces, enforcing authentication requirements for all command paths, and maintaining access control lists for onboard processors, flight software, and payload interfaces.
Parent ID: SV-AC
Family: SV-AC

## VALID OUTPUT EXAMPLE
{
  "pairs": [
    {
      "question": "What is SV-AC-1 Spacecraft Access Control according to SPARTA?",
      "reasoning": "The description explicitly defines the access control requirements for spacecraft systems.",
      "answer": "SV-AC-1 Spacecraft Access Control requires implementing logical access controls to spacecraft systems to ensure only authorized personnel and processes can interact with spacecraft components and data.",
      "pair_type": "countermeasure_description",
      "control_id": "SV-AC-1",
      "evidence_quotes": [
        {
          "quote": "The organization implements logical access controls to spacecraft systems to ensure only authorized personnel and processes can interact with spacecraft components and data.",
          "relevance": "Core requirement of the countermeasure."
        }
      ],
      "confidence": "high",
      "actionable_for": "implementation"
    },
    {
      "question": "What control family does SV-AC-1 Spacecraft Access Control belong to?",
      "reasoning": "The family field explicitly identifies the control family.",
      "answer": "SV-AC-1 Spacecraft Access Control belongs to the SV-AC control family.",
      "pair_type": "family_context",
      "control_id": "SV-AC-1",
      "evidence_quotes": [
        {
          "quote": "SV-AC",
          "relevance": "Value of family field."
        }
      ],
      "confidence": "high",
      "actionable_for": "training"
    },
    {
      "question": "What scope does SV-AC-1 Spacecraft Access Control apply to?",
      "reasoning": "The description explicitly identifies the target systems and interfaces.",
      "answer": "SV-AC-1 applies to ground segment commanding interfaces, all command paths, onboard processors, flight software, and payload interfaces.",
      "pair_type": "scope_clarification",
      "control_id": "SV-AC-1",
      "evidence_quotes": [
        {
          "quote": "ground segment commanding interfaces, enforcing authentication requirements for all command paths, and maintaining access control lists for onboard processors, flight software, and payload interfaces",
          "relevance": "Explicit scope definition."
        }
      ],
      "confidence": "high",
      "actionable_for": "audit"
    }
  ],
  "skipped_reason": null
}

## INVALID OUTPUT EXAMPLES

### Invalid Example 1: Duplicate aspect coverage
{
  "pairs": [
    {
      "question": "What is SV-AC-1?",
      "pair_type": "countermeasure_description",
      "answer": "..."
    },
    {
      "question": "How does SV-AC-1 work?",
      "pair_type": "countermeasure_description",
      "answer": "..."
    }
  ],
  "skipped_reason": null
}
WHY INVALID:
Both pairs cover the same aspect. Definition and mechanism belong in one countermeasure_description pair.

### Invalid Example 2: Invented NIST relationship
{
  "pairs": [
    {
      "answer": "SV-AC-1 is derived from NIST SP 800-53 AC-2 Account Management."
    }
  ],
  "skipped_reason": null
}
WHY INVALID:
The source does not explicitly state any NIST mapping in the description field.

### Invalid Example 3: Ungrounded explanatory context
{
  "pairs": [
    {
      "answer": "Access control is essential for protecting spacecraft from cyber attacks that could disrupt mission operations."
    }
  ],
  "skipped_reason": null
}
WHY INVALID:
That explanatory sentence is not present in the admissible source fields.

### Invalid Example 4: Inferred implementation advice
{
  "pairs": [
    {
      "pair_type": "implementation_guidance",
      "answer": "Organizations should implement role-based access control using LDAP and hardware security modules."
    }
  ],
  "skipped_reason": null
}
WHY INVALID:
The description does not explicitly mention LDAP or hardware security modules.

### Invalid Example 5: Strengthened modality
{
  "pairs": [
    {
      "source": "should implement access controls",
      "answer": "must implement access controls"
    }
  ],
  "skipped_reason": null
}
WHY INVALID:
The answer strengthens "should" (recommendation) to "must" (requirement).
