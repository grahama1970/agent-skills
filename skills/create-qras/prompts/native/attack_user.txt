Return exactly one JSON object with keys: pairs, skipped_reason

## Task
Generate up to 6 grounded, non-duplicative QRA pairs for {framework} technique {control_id} ({control_name}).

## Source Content

### Technique Document
{control_details}

### Supplementary Content (from linked URLs)
{supplementary_content}

## Example — VALID output

Input technique:
  Technique ID: T1595
  Name: Active Scanning
  Framework: ATT_CK_Enterprise
  Description: Adversaries may execute active reconnaissance scans to gather information that can be used during targeting. Active scans are those where the adversary probes victim infrastructure via network traffic, as opposed to other forms of reconnaissance that do not involve direct interaction.

Output:
{{
  "pairs": [
    {{
      "question": "What is T1595 Active Scanning according to MITRE ATT&CK?",
      "reasoning": "The description defines Active Scanning as reconnaissance via network probing.",
      "answer": "T1595 Active Scanning is a reconnaissance technique where adversaries may execute active reconnaissance scans to gather information that can be used during targeting. Active scans are those where the adversary probes victim infrastructure via network traffic, as opposed to other forms of reconnaissance that do not involve direct interaction.",
      "pair_type": "threat_description",
      "control_id": "T1595",
      "evidence_quotes": [
        {{"quote": "Adversaries may execute active reconnaissance scans to gather information that can be used during targeting", "relevance": "Primary definition"}},
        {{"quote": "Active scans are those where the adversary probes victim infrastructure via network traffic, as opposed to other forms of reconnaissance that do not involve direct interaction", "relevance": "Distinguishes active from passive reconnaissance"}}
      ],
      "confidence": "high",
      "actionable_for": "training"
    }},
    {{
      "question": "How can T1595 Active Scanning be detected?",
      "reasoning": "Detection section describes monitoring for uncommon network data flows.",
      "answer": "Detect T1595 Active Scanning by monitoring network data for uncommon data flows. Processes utilizing the network that do not normally have network communication or have never been seen before are suspicious.",
      "pair_type": "detection_method",
      "control_id": "T1595",
      "evidence_quotes": [
        {{"quote": "Monitor network data for uncommon data flows. Processes utilizing the network that do not normally have network communication or have never been seen before are suspicious.", "relevance": "Verbatim detection guidance from ATT&CK"}}
      ],
      "confidence": "high",
      "actionable_for": "implementation"
    }}
  ],
  "skipped_reason": null
}}

## INVALID OUTPUT — examples to avoid

### Invalid Example 1: Duplicate coverage (definition/mechanism split)
{{
  "pairs": [
    {{"question": "What is {control_id}?", "pair_type": "threat_description", "answer": "{control_id} is {control_name}, a reconnaissance technique..."}},
    {{"question": "How does {control_id} work?", "pair_type": "threat_description", "answer": "{control_id} works by probing victim infrastructure..."}}
  ]
}}
WHY INVALID: Both pairs have pair_type "threat_description". Definition and mechanism are the SAME aspect — do not split them into separate pairs.

### Invalid Example 2: Contaminated evidence
{{
  "evidence_quotes": [{{"quote": "Star 55 forks 13 Branches Tags Activity"}}]
}}
WHY INVALID: This is GitHub page chrome, not ATT&CK content. Evidence must come from admissible sources only.

### Invalid Example 3: Modality strengthening
{{
  "source": "adversaries may use X",
  "answer": "adversaries use X"
}}
WHY INVALID: "may" indicates possibility; "use" indicates certainty. Preserve the hedge.

### Invalid Example 4: Paraphrasing instead of verbatim
{{
  "source": "gather information that can be used during targeting",
  "answer": "gather information for targeting"
}}
WHY INVALID: The answer compresses the source. Use the full verbatim phrase.

## pair_type Decision Rules (ATT&CK techniques)

For ATT&CK techniques, use ONLY these pair_types:
- threat_description: What the technique is, how it works (from description)
- detection_method: How to detect it (from Detection section)
- mitigation_guidance: How to mitigate it (from Mitigations table)
- scope_clarification: When/where it applies, what platforms (from metadata)
- risk_context: What threat actors use it, real-world examples (from Procedure Examples)

Do NOT use implementation_guidance or assessment_criteria for ATT&CK techniques — those are for NIST/SPARTA controls only.

## Source Precedence (highest to lowest)
1. Technique Document description — authoritative definition
2. ATT&CK technique page sections (Mitigations, Detection) — official MITRE content
3. Procedure Examples table — real-world usage (cite the threat actor name in the answer)

If sources conflict, use the higher-precedence source. Ignore GitHub and other inadmissible content entirely.

## Output Schema
{{
  "pairs": [
    {{
      "question": "string — must include {control_id} and technique name",
      "reasoning": "string — 1-2 sentences explaining why this Q&A is valid, max 50 words",
      "answer": "string — 1-4 sentences grounded in source, max 150 words",
      "pair_type": "one of: threat_description | detection_method | mitigation_guidance | scope_clarification | risk_context",
      "control_id": "{control_id}",
      "evidence_quotes": [
        {{"quote": "verbatim text from admissible source, 5-150 words", "relevance": "why this supports the answer"}}
      ],
      "confidence": "high | medium",
      "actionable_for": "implementation | audit | risk_assessment | training"
    }}
  ],
  "skipped_reason": "string explaining why zero pairs, or null if pairs generated"
}}

## Insufficiency Rules — Return 0 Pairs When:
1. The admissible content (after filtering out GitHub, page chrome, metadata) contains fewer than 2 distinct factual claims
2. Content is only metadata (IDs, dates, links) with no substantive description of the technique
3. You cannot find a verbatim quote from an admissible source to support any answer
4. All available content is contaminated (e.g., only GitHub pages, no ATT&CK technique description)

When returning zero pairs, skipped_reason must explain which insufficiency rule applies.

## Hard Constraints
1. Maximum 6 pairs per technique
2. Every question must include "{control_id}" and the technique name
3. Every answer must have at least one evidence_quote from an admissible source
4. Each distinct claim in the answer must be supported by an evidence_quote
5. confidence="high" requires verbatim quote; "medium" requires close paraphrase from admissible source
6. Do NOT use confidence="low" — if you cannot find source support, do not generate the pair

## Distinctness Requirement
Each pair must cover a DIFFERENT aspect. The 5 valid pair_types for ATT&CK are:
- threat_description: Definition AND mechanism (these are ONE aspect — do not split)
- detection_method: How to detect
- mitigation_guidance: How to mitigate
- scope_clarification: Platforms, tactics, applicability
- risk_context: Real-world threat actor usage

You may generate AT MOST one pair per pair_type. If the source only supports 2 aspects, generate only 2 pairs. Generating a second pair for the same aspect (e.g., two threat_description pairs) violates this requirement.
