Return exactly one top-level JSON object with keys:
- pairs
- skipped_reason

Generate up to 4 grounded, non-duplicative QRA pairs from this documentation excerpt.

## Document

URL ID: {url_id}
Topic: {topic}

## Content

{text}

## Reminders

- Use only the content shown above.
- Topic may shape the question but is NOT evidence.
- Every claim must be directly supported by the text.
- Preserve modality exactly.
- Prefer fewer pairs over weak pairs.
- If source support is insufficient, return zero pairs.
