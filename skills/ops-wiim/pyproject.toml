[project]
name = "ops-wiim"
version = "0.1.0"
description = "WiiM Amp local-network diagnostics for low-volume triage (read-mostly)."
requires-python = ">=3.11,<3.13"
dependencies = [
    "typer>=0.12.0",
    "httpx>=0.27.0",
    "loguru>=0.7.0,<0.7.3",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
