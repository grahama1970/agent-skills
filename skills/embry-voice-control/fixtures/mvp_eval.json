{
  "version": 2,
  "skill": "embry-voice-control",
  "trials": 2,
  "proof_scope": "two-agent MVP: live Agent A (speaker) + live Agent B (solver) over a real solver_event.v1 JSONL stream",
  "claims": {
    "proves": "A and B run concurrently on a real stream; B solves via memory /answer, A tails real log offsets, speaks a cover before B finishes, and renders B's actual answer via Chatterbox; a dead solver endpoint fails the loop closed",
    "does_not_prove": "barge-in, live listening, fast-model cover generation, or perceived voice quality"
  },
  "capability_claims": [
    {
      "id": "embry_voice_control.two_agent_mvp",
      "description": "the fast speaker and slow solver run concurrently over a real solver_event.v1 log; A covers before B finishes and delivers B's real answer",
      "criticality": "critical",
      "claim_semantics": "control_flow",
      "evidence_required": {"live_e2e": true}
    }
  ],
  "cases": [
    {
      "name": "two-agent-loop-live",
      "type": "positive",
      "real_world": true,
      "evidence_class": "live_e2e",
      "supports_claims": ["embry_voice_control.two_agent_mvp"],
      "command": ["python3", "../scripts/mvp/run_mvp.py", "What does control SC-7 require?", "sparta"],
      "expected": {"exit_code": 0, "stdout_contains": ["PASS: two-agent MVP loop", "[OK] a_spoke_before_b_done", "[OK] answer_matches_b"], "stdout_excludes": ["FAIL:"]}
    },
    {
      "name": "dead-solver-fails-closed",
      "type": "adversarial",
      "real_world": true,
      "evidence_class": "fault_injected_deterministic",
      "supports_claims": ["embry_voice_control.two_agent_mvp"],
      "command": ["bash", "-lc", "EMBRY_MEM_URL=http://127.0.0.1:9 python3 ../scripts/mvp/run_mvp.py 'What does control SC-7 require?' sparta"],
      "expected": {"exit_code": 1, "stdout_contains": ["FAIL: two-agent MVP loop"]}
    }
  ]
}
