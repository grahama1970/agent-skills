# Operator handoff

Start with a network-enabled local checkout. Resolve dependencies and commit the
actual uv lock; run the full browser profile under your normal Chromium policy.
Do not loosen managed browser policy merely to obtain a green report.

Point `AGENT_SKILLS_ROOT` at the real `grahama1970/agent-skills` checkout. Run native
setup plan/audit and mechanism/full evaluation. Let those owning tools validate
fixture compatibility and report missing claims; do not replace them with local
status files. Review and approve the draft goal before signing any acceptance basis.

Admit a licensed, consented real corpus with reliable authors/task/repository
relationships. Train the baseline, inspect calibration failures and held-out
performance, then run the numeric gate at an approved operating point. Add an
independent reviewer workflow and real model-backed browser case before claiming
product-level readiness. These evidence slots intentionally remain unfilled.

The next algorithmic experiments should compare a true UniXcoder multi-view model,
the local contrastive score, and a carefully verified code-aware perturbation
method against the shipped baseline under the same frozen splits. Add multilingual
support only with parsers, appropriate features and language-specific evaluation.
Do not present researched watermark schemes as implemented detection coverage.

Use source-bound failures to create focused agent-work tickets in your own workflow;
this bundle neither creates GitHub issues nor publishes to a repository.
