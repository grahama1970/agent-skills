[project]
name = "assistant-lab"
version = "1.0.0"
description = "Self-improvement workbench for /assistant cascade models"
dependencies = [
    "PySide6>=6.6.0",
    "loguru>=0.7,<0.7.3",
    "typer>=0.9",
]

requires-python = ">=3.10,<3.13"
