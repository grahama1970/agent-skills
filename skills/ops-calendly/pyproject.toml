[project]
name = "ops-calendly"
version = "0.1.0"
description = "Calendly API ops for grahama.co scheduling"
requires-python = ">=3.11,<3.13"
dependencies = [
    "httpx>=0.27",
    "loguru>=0.7",
    "pydantic>=2.0",
    "python-dotenv>=1.0",
    "typer>=0.12",
]

[tool.uv]
package = false
