[project]
name = "batch-quality"
version = "0.1.0"
description = "Preflight validation and quality gates for batch LLM operations"
requires-python = ">=3.10,<3.13"
dependencies = [
    "duckdb>=1.4.4",
    "typer>=0.9.0",
    "loguru>=0.7.0,<0.7.3",
    "python-dotenv>=1.0.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=7.0.0",
]
