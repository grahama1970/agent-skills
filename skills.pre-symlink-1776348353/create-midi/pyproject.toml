[project]
name = "create-midi"
version = "0.1.0"
description = "Compose MIDI arrangements + JSON/MIDI conversion"
requires-python = ">=3.10,<3.13"
dependencies = [
    "typer>=0.9.0",
    "loguru>=0.7.0,<0.7.3",
    "httpx>=0.28.0",
    "pretty_midi>=0.2.10",
]
