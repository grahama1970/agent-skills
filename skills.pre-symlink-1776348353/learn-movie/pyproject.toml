[project]
name = "pi-skill-learn-movie"
version = "0.1.0"
description = "Runtime package for the learn-movie skill"
requires-python = ">=3.11,<3.13"
dependencies = [
    "httpx>=0.24.0",
    "loguru>=0.7.0,<0.7.3",
    "typer>=0.9.0",
]
