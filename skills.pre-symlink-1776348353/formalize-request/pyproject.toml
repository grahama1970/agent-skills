[project]
name = "pi-skill-formalize-request"
version = "0.1.0"
description = "Runtime package for the formalize-request skill"
requires-python = ">=3.11,<3.13"
dependencies = [    "python-dotenv>=1.0.0",
    "typer>=0.9.0",
]
