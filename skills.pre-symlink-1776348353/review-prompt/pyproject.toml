[project]
name = "review-prompt"
version = "0.1.0"
description = "Multi-model prompt review loop via /scillm batch"
requires-python = ">=3.11,<3.13"
dependencies = [
    "httpx>=0.27",
    "typer>=0.12",
    "loguru>=0.7,<0.7.3",
]

