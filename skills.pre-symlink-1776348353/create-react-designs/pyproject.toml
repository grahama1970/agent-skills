[project]
name = "pi-skill-create-react-designs"
version = "0.1.0"
description = "Runtime package for the create-react-designs skill"
requires-python = ">=3.11,<3.13"
dependencies = [    "typer>=0.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
