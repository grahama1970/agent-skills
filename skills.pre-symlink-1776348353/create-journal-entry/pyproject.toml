[project]
name = "pi-skill-create-journal-entry"
version = "0.1.0"
description = "Runtime package for the create-journal-entry skill"
requires-python = ">=3.11,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
