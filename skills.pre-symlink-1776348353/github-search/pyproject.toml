[project]
name = "github-search"
version = "0.1.0"
description = "Deep multi-strategy GitHub search for repositories and code"
requires-python = ">=3.11,<3.13"

dependencies = [
    "typer>=0.9.0",
    "rich>=13.0.0",
]
