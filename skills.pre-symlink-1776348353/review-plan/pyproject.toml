[project]
name = "review-plan"
version = "1.0.0"
description = "Validate task files before orchestration"
requires-python = ">=3.11,<3.13"

dependencies = [
    "loguru>=0.7.0,<0.7.3",
    "typer>=0.9.0",
    "pyyaml>=6.0.2",
]
