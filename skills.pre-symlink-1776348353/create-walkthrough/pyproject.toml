[project]
name = "pi-skill-create-walkthrough"
version = "0.1.0"
description = "Runtime package for the create-walkthrough skill"
requires-python = ">=3.11,<3.13"
dependencies = [    "loguru>=0.7.0,<0.7.3",
    "python-dotenv>=1.0.0",
    "typer>=0.9.0",
]
