[project]
name = "pi-skill-persona-journal"
version = "0.1.0"
description = "Runtime package for the persona-journal skill"
requires-python = ">=3.11,<3.13"
dependencies = [    "loguru>=0.7.0,<0.7.3",
]
