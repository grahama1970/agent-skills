# DEPRECATED — Archived 2026-04-02

`/subagent-service` has been replaced by:
- **`/code-runner`** — for iterative code writing (self-improvement loop)
- **`/scillm`** — for one-shot LLM inference (classification, extraction)

Archived to: `.archive/skills/subagent-service/`

Reason: Headless `claude -p` and `codex exec` calls proved unreliable (0% file write success).
All active callers have been migrated.
