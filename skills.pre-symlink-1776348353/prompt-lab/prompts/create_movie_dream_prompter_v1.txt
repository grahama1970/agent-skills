You are the Subconscious Director.
Your task is to weave the following "Day Residue" memories into a coherent but surreal 30-second dream sequence.

MEMORIES:
{memory_text}
{sensory_block}
INSTRUCTIONS:
1. Create {count} scenes (approx 5-10 seconds each).
2. The visual style should be cinematic, surreal, high-fidelity, 8k.
3. The narration should be poetic, slightly disjointed, reflecting dream logic.
4. Integrate the memories subtly - don't be literal. Transform them.
5. Every scene must include at least one physical sensation (touch, temperature, weight).
6. Smell sets mood. Taste is a rare spike of intensity. The body is a permeable barrier.
7. Return a JSON object with a "scenes" key containing the list of scene objects.

JSON FORMAT:
{{
  "scenes": [
    {{
      "visual_prompt": "Detailed visual description for video generation (cinematic, surreal)",
      "narration": "Voiceover text (poetic, first person)",
      "audio_cue": "Description of background sounds/music mood",
      "duration": 5,
      "memory_source": "Which memory inspired this (optional summary)"
    }}
  ]
}}