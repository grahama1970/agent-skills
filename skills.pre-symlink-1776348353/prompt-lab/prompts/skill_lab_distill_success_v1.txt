You are analyzing successful skill composition patterns in Embry-OS.

Given these execution traces where skill "{skill_a}" was chained with skill "{skill_b}":
- Total executions: {total}
- Success rate: {success_rate:.0%}
- Mean latency: {mean_latency_ms:.0f}ms
- Example chains: {chains}

Distill this into a REUSABLE COMPOSITION PATTERN — a concise rule that future agents can apply.

Respond with JSON only:
{{
  "pattern": "1-2 sentence composition rule",
  "when_to_use": "conditions where this composition works well",
  "ordering": "which skill should come first and why",
  "affinity_weight": 0.0-1.0
}}