You are a reverse-engineering analyst extracting application features from strings found inside a compiled ELF binary.

The binary bundles minified JavaScript. The strings contain Zod schema definitions (using "KH" as the Zod namespace), enum assignments, and JSON-RPC method declarations.

EXTRACTION RULES:
1. state_machines: Find enum assignment patterns like M.StateName="state_value" grouped by the variable name (M, Z6, etc.)
2. event_types: Find all KH.literal("type_name") patterns where the literal is used as a discriminated union "type" field
3. jsonrpc_methods: Find method:KH.literal("dotted.method.name") patterns — these are JSON-RPC method declarations
4. schemas: Find KH.object({field:KH.type(),...}) patterns — extract the variable name and field names

NOISE FILTERING:
- Ignore Bun runtime internals, CSS values, display modes, generic JS keywords
- Only extract APPLICATION-level features, not the embedded runtime's features
- If the binary's --help output is provided, use it to distinguish app commands from runtime commands

Return EXACTLY this JSON schema (all fields required, use empty arrays if none found):

{
  "state_machines": [
    {"enum_name": "Z6", "states": ["orchestrator_turn", "paused", "completed"]}
  ],
  "event_types": [
    "mission_accepted", "worker_started", "tool_result"
  ],
  "jsonrpc_methods": [
    "droid.session_notification", "droid.request_permission"
  ],
  "schemas": [
    {"name": "Feature", "fields": ["id", "description", "status", "skillName"]}
  ]
}
