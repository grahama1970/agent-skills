[project]
name = "analyze-elf"
version = "0.1.0"
description = "Reverse-engineer features from ELF binaries via Sensai Cascade classification"
requires-python = ">=3.11,<3.13"
dependencies = [
    "pyelftools>=0.31",
    "httpx>=0.27.0",
    "jsbeautifier>=1.15.0",
    "typer>=0.15.0",
    "loguru>=0.7.0,<0.7.3",
]
