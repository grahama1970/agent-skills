[project]
name = "pi-skill-best-practices-python"
version = "0.1.0"
description = "Runtime package for the best-practices-python skill"
requires-python = ">=3.11,<3.13"
dependencies = [
    "loguru>=0.7.0,<0.7.3",
    "rich>=13.0.0",
    "tomli",
    "typer>=0.9.0",
]

[tool.hatch.build.targets.wheel]
packages = []
