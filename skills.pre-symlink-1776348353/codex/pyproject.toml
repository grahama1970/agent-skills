[project]
name = "codex"
version = "0.1.0"
description = "High-reasoning agentic bridge via OpenAI Codex CLI (gpt-5.3-codex)"
requires-python = ">=3.11,<3.13"

dependencies = [
    "typer>=0.9.0",
    "loguru>=0.7.0,<0.7.3",
]
